# 学習ジャーナル

Python / AI駆動開発の学習記録。note記事の素材として、うまくいったことだけでなく
詰まった点・気づきもそのまま残す。

---

## 2026-07-12

### やったこと
- リポジトリ `learning-python` を作成（`git init`のみ、コミットはまだ）
- `lesson01_basics/practice.py`: 町丁目人口データの集計サンプル（変数・型・for文・if文）
  COBOL/FORTRANとの対比で解説
- `tennis_stats/tennis_stats.py`: テニス試合結果集計プログラムを作成
  - データ構造: `{"opponent": 名前, "sets": [(自分, 相手), ...]}`
  - 関数: `did_win`, `win_rate`, `win_rate_by_opponent`, `game_win_rate`
  - 当初はハードコードのリストだったが、`input()`による対話入力版に変更

### 詰まった点・気づき
- Windows環境でGit Bash経由でPythonを実行すると、日本語出力が文字化けした
  → `setx PYTHONUTF8 1` で恒久設定（次回起動セッションから有効）

### 次回予定
- テニス集計プログラムの続き（自分でキーボード入力して動作確認 / CSV等の外部ファイル読み込み版への拡張）

---

## 2026-07-15

### やったこと
- （セッション開始時に前回の記憶が引き継がれずコケた件を扱った回）

### 詰まった点・気づき
- 起動フォルダを間違え、`C:\Users\matsu`から起動した前回セッションの記憶が、
  別セッション（`AIPractice`フォルダから起動）からは見えなかった
  → 起動フォルダとメモリの紐付きを意識する必要がある、という気づき
- エディタの見方がよくわからず戸惑った（詳細未記録）
- 学習の本当の目的が「Pythonスキル習得」だけでなく、
  「還暦手前の開発マネージャがAI駆動開発を実際に手を動かして学んだときの躓き・気づきをnote記事にする」
  ことにあると明言。以降、詰まった点も含めて記録する方針に

---

## 2026-07-25

### やったこと
- 間が空いたのでセッション冒頭でこれまでの進捗をおさらい
- 詰まった点のログが記憶（メモリ）の中にしか残っていなかったため、
  リポジトリ内に振り返り用の `journal.md` を新設

### 詰まった点・気づき
- 「躓いた点を記録しておいてほしい」という依頼は覚えていたが、
  それが自分（Claude）の内部メモリにしか保存されておらず、
  本人が直接読める形になっていなかった
  → 本人が見返せる場所（リポジトリ内のファイル）に残すことの重要性に気づく

- テニス集計プログラムをコマンドプロンプトで実際に動作確認
  - 対戦相手「シナー」に対し `6-1,6-4,5-7` と入力（1行）→ 勝率が100%と表示され、
    「67%になるはず」という感覚とズレて「バグでは」と感じた
  - 原因を確認したところ、このプログラムは「1行＝1試合」という仕様で、
    カンマ区切りは同じ試合内のセット（例: 3セットマッチなら3組）を表す設計だった
  - 入力した`6-1,6-4,5-7`は「3セットマッチを1試合分入力した」ことになり、
    3セット中2セット取って試合に勝った→1試合中1勝＝100%という、仕様通り正しい結果だった

### 詰まった点・気づき
- アマチュアのテニスは1セットマッチがほとんどのため、「1行に複数セットを書く」という
  発想がそもそもなかった。仕様（1行＝1試合、カンマ区切り＝セット）を知らずに入力すると、
  「セット単位の勝率のつもり」で数字を見てしまい、結果とズレて「バグかも」と感じてしまう
  → 自分が入力しているデータの単位（試合なのかセットなのか）を意識しないまま数値を見ると、
    正しく動いているプログラムでも「間違っている」と誤解しうる、という気づき
  → 5セットマッチなら `6-1,6-3,3-6,4-6,7-6` のように5組を1行に書けばよい、という理解に到達

- 現実にはあり得ないスコアを入力して、プログラムがどこまで許容するかを試した
  - 例1: `6-3,6-3,2-6`（3セットマッチで最初の2セットを先取した時点で試合は終了するはずなのに、
    3セット目まで入力できてしまう＝矛盾したデータ）
  - 例2: `6-3,4-2`のように、片方が6ゲームに到達しないままセットが終わっている
    （通常はあり得ず、負傷等による途中棄権(リタイア)か、降雨等によるサスペンデッドのはず）
  - 現状のプログラムはスコアのバリデーションを一切行っておらず、どちらのケースもそのまま
    集計されてしまうことが分かった

### 詰まった点・気づき
- 「動くかどうか」だけでなく「あり得ないデータをどう扱うか」を試す視点は、
  開発マネージャとして要件定義・テスト設計をしてきた経験がそのまま活きる場面だった
- 実運用を意識すると、リタイア・サスペンデッドのような「スコアでは表現できない試合結果」を
  どう入力させるか（例: `DEF`という特別な表記を用意する等）という設計判断が必要になる、
  という気づきに繋がった。単純な数値集計だけでは現実の運用に耐えないケースがある

- バリデーション追加は今回は見送り、既知の制約事項として記録するに留める判断をした
  （優先度としてはCSV読み込み版などの拡張を先に進める）

- CSV読み込み版への拡張を実施
  - 設計確認: CSV列構成は「対戦相手,セットまとめ1列」（例: `シナー,"6-1,6-4,5-7"`）を採用、
    対話入力とCSV読み込みは両方残し起動時に選択する方式に決定
  - `input_matches_from_csv(filepath)`関数を追加。既存の`parse_sets`をそのまま再利用でき、
    集計ロジック側は無変更で済んだ（設計当初の狙い通り、入力部分の差し替えだけで対応できた）
  - 動作確認用に`tennis_stats/sample_matches.csv`を作成し、実行確認済み
  - matsuさん自身でもCSVファイルを作成して試す予定（次回に持ち越し）

### 次回予定
- 自分で作ったCSVファイルでの動作確認
- （時間があれば）既知の制約事項（バリデーション未対応）への対応を検討

### 既知の制約事項（未対応）
- スコアのバリデーションを行っていないため、以下のような現実にはあり得ない入力もそのまま集計されてしまう
  - 矛盾したセット数（例: 3セットマッチで2セット先取後も3セット目が続く `6-3,6-3,2-6`）
  - 6ゲーム未満でセットが終了している（リタイア・サスペンデッド等、通常のスコアでは表現できないケース）
  - 対応案: リタイア等を表す `DEF` のような特別な表記を用意する 等

---

## 2026-08-02

### やったこと
- データ標準化・データマネジメントへの興味をきっかけに、バリデーション未対応の制約事項に着手
- 設計方針を確認してから実装（AskUserQuestionで2回確認）
  1. 妥当性チェックに引っかかったデータの扱い方: 「隔離してレポート」を採用
     （ETLでいう「リジェクト/隔離」パターン。正常データだけ集計を継続し、
     問題のあった行は理由付きで別途レポート）
  2. 検証範囲: 今回は「セット単位のスコアのみ」（試合単位の整合性チェックは見送り、次回以降の課題）
- 隔離したデータの扱いについて、「異常値として除外」と「表記ゆれとして正規化し集計対象に戻す」の
  2種類があるのでは、という指摘があり、3段階の分類モデルで実装することにした
  - `valid`（正常）: そのまま集計
  - `corrected`（補正）: 全角数字（`６-３`等）を半角に正規化して採用。
    何をどう補正したかは必ずログに残す（黙って書き換えない）
  - `rejected`（却下）: 正規化しても妥当なスコアにならないもの（`6-6`など存在しないスコア、
    `7-x`など数値化できないもの）。行ごと集計から除外し、元データ（生の値）と理由をレポートに残す
- `tennis_stats.py`に`normalize_score_text`（全角→半角変換）、`is_valid_set`（セットの妥当性判定）、
  `parse_set_token`（1セット単位の分類）、`classify_line`（1試合＝1行単位の分類）を追加
- `input_matches_from_csv`を改修し、`matches`に加えて`corrected_log`・`rejected_log`を返すように変更
  （CSV読み込み経路のみ対応。対話入力は対象外のまま）
- わざと表記ゆれ・異常値を混ぜた`sample_matches_dirty.csv`を作成して動作確認
  - 全角スコア → 補正ログに記録された上で集計に反映
  - `6-6`・`7-x` → 却下ログに記録され、集計から除外
  - 既存の`sample_matches.csv`（正常データのみ）でも結果が変わらないことを確認（リグレッションなし）

### 詰まった点・気づき
- 「バリデーション」を一枚岩で考えず、「除外すべき異常値」と「正規化すれば救えるデータ」を
  区別するという発想は、データマネジメント業務そのものだと感じた
  （マネージャとしての実務感覚が、Pythonの実装設計にそのまま活きた場面）
- 生データを残さず黙って補正してしまうと、あとから「なぜその数字になったか」を追跡できなくなる、
  という点は開発マネージャとしての経験（監査証跡の重要性）とも一致していた

### 次回予定
- 対話入力（キーボード）側にも同様の検証を入れるかどうかの判断
- 見送っていた「試合単位の整合性チェック」（矛盾したセット数など）への対応検討
- リタイア・サスペンデッド用の`DEF`表記対応の検討
- ダッシュ文字違い・余分な空白など、他の表記ゆれへの補正拡張を検討するかどうか

### 詰まった点・気づき（コミット時）
- リポジトリ作成（2026-07-12のgit init）以来、一度もコミットしていなかったことに気づき、
  ここで初コミットを実施しようとしたところ「Author identity unknown」エラーで失敗
  → `git config --global user.name` / `user.email` が未設定だったことが原因
- この設定はプロジェクト単位ではなくPC（またはアカウント）につき1回でよい設定で、
  本来はGitインストール直後・最初のコミット前にやっておくべきものだった
  - コマンドライン版Gitにはこの設定を促す専用画面が存在しない
    （GitHub Desktop等のGUIツールなら初回セットアップ時に自動で聞かれる）
  - そのため「最初にコミットしようとした瞬間に初めてエラーで気づく」という経験をした
  - `user.name`を`matsu64000`、`user.email`を`matsu64000@gmail.com`に設定して解決
- 気づき: 公開リポジトリにする場合、commitに記録されるemailは誰でも閲覧可能になる点も
  合わせて説明を受けた（GitHubの`users.noreply.github.com`形式で回避可能）。
  今回は学習用ローカルリポジトリのため本メールアドレスのまま進行、公開時に再検討する方針
- 初コミット `d0884c5`（6ファイル、412行）完了

### 番外: Claude Codeの権限確認プロンプトについての気づき
- コミット前に出てくる「Do you want to proceed?」の確認プロンプトが、
  「コードレビューの依頼」なのか疑問に思い質問した
- 実際は「実行許可（メカニズム）」であり、「コードレビュー（中身の品質確認）」とは別物、
  という説明を受けた。AI駆動開発には
  1. 実行許可（設定ファイルによる許可リスト。ツール単位・コマンドパターン単位で設定可能）
  2. 判断基準（破壊的操作やpublicな領域への影響がある場合は、許可設定があっても都度確認する
     という行動規範）
  の2層構造があるとのこと
- 自治体システムの「アクセス権限テーブル」と「本番リリースのCAB承認」の対比で腹落ちした
  （権限があること＝実行してよいこと、ではないという整理）

---

## 2026-08-09

### やったこと
- 前回積み残していた「対話入力（キーボード）側にもバリデーションを入れるか」に着手する前に、
  バリデーションの考え方そのものを整理した
  - 疑問: 本来「エンティティの制約（属性・コード値範囲等）」を定義すべき一方で、
    「入力段階（キーボード／外部ファイル／その他）でのエラー検知と対策
    （オミット／アラートして修正機会を与える／一定ルールで自動修正）」も必要になる。
    Python的セオリーはどうなるか、Claudeを使うときもこの考え方をインプットすべきか、を質問
  - 整理した内容:
    1. 「エンティティの制約（何が妥当なデータか）」と「境界での対応（検知後どう反応するか）」は
       本来別物。COBOLのPICTURE句・コピー本による項目定義と、READ文後に自分で書くIF文による
       妥当性チェックが別物だったのと同じ構造
    2. 「信頼境界（trust boundary）」の外から来るデータは疑い、内側では信頼するという考え方が定石。
       境界（キーボード・CSV・将来のAPI等）は複数あっても、「何が妥当か」の判定ロジックは
       1箇所にまとめ、各境界は判定結果への「対応」だけを担当する、という分離をする
    3. 実は`tennis_stats.py`は既にこの形になっていた
       （`is_valid_set`/`parse_set_token`/`classify_line`が入力元非依存の判定ロジック、
       `input_matches_from_csv`が境界側の対応）。気づかないうちにセオリー通りの設計になっていた
    4. 判定ロジックは共通化する一方、検知後の「対応」はチャネルの特性で変えるのが自然
       （バッチ処理＝CSVは隔離してレポート、対話的＝キーボードはその場でエラーを伝えて
       再入力を促す、が定石）
    5. Claudeへのインプットについて: 積極的に伝えるべき。上記1〜4は実質的に要件定義そのもので、
       先に言葉にして渡すことで実装が「判定ロジックの一元化＋チャネル別の対応」という構造に
       なりやすい。言わずに「バリデーションして」とだけ頼むと、各所に場当たり的なチェックが
       分散しがちになる
  - この整理を踏まえ、対話入力での不正スコアへの対応方針を確認
    （即座にエラーを伝えて再入力させる／一通り入力後にまとめてエラー一覧を表示、の2択）
    → 対話的なチャネルの特性に合わせて「即座にエラーを伝えて再入力させる」を採用

### やったこと（実装）
- `input_matches`（キーボード対話入力）を改修し、CSV側と同じ`classify_line`を呼ぶように変更
  - `rejected`: その場でエラー内容を表示し、同じ行を再入力させる
  - `corrected`: 補正内容を表示した上で採用する
  - 判定ロジックそのものは新規に書かず、CSV側と完全に共有（この共通化自体が今回整理した
    セオリーの実践）
- 上記により、対話入力専用だった`parse_sets`関数が不要になったため削除
- 動作確認
  - CSV経路（`sample_matches.csv`）: 結果が変わらないことを確認（リグレッションなし）
  - 対話入力: `6-6`（存在しないスコア）→ エラー・再入力、`7-x`（数値化不可）→ エラー・再入力、
    `６-３,4-6,6-2`（全角混じり）→ 補正して採用、という一連の流れを確認

### 詰まった点・気づき
- 「バリデーションをどう書くか」より先に「バリデーションの構造をどう考えるか」を言語化したことで、
  実装そのものは非常に小さな変更（既存の判定関数を呼ぶだけ）で済んだ。設計判断に時間をかけると
  実装が軽くなる、という開発マネージャーとしての感覚がここでも裏付けられた
- 「要件定義を先に渡すとAIの実装がぶれない」という点は、部下に仕様を依頼するときの感覚と
  ほぼ同じだと感じた

### 次回予定
- 見送っていた「試合単位の整合性チェック」（矛盾したセット数など）への対応検討
- リタイア・サスペンデッド用の`DEF`表記対応の検討
- ダッシュ文字違い・余分な空白など、他の表記ゆれへの補正拡張を検討するかどうか

---

## 2026-08-09（続き）

### やったこと
- Python初心者としてのコードレビューの限界について質問
  - 整理: レビューには「要求適合性／動作結果／構造・設計／言語仕様の細部」の4層があり、
    経験がものを言う1〜3層は今の実力でも十分レビューできる。言語仕様の細部（4層目）は
    今は無理に見ようとせず、気になった書き方をその都度質問することで少しずつ広げればよい
- テストケースの妥当性・カバレッジについて質問
  - 整理: カバレッジ（行の実行割合）は正しさの保証ではない。妥当性の判断軸は実務でも使う
    「同値分割・境界値分析」がそのまま使える。カバレッジツールは「洗い出し漏れの発見」に
    使う補助であり、目標値にはしない
  - テストコード自体は「入力→期待する出力」という形で明示されるため、Python文法に詳しくなくても
    「テストが仕様を正しく表現しているか」はレビューできる、という実践的な戦略に至った
- カバレッジ基準（C0/C1/C2/MC-DC）について、COBOL/FORTRANのような手続き型言語とPython
  （特に今回のような分離を前提とした設計）とで考え方が変わるのではという質問
  - 整理: これらは全て「制御フローグラフ上のどこを通ったか」を測る構造カバレッジ。COBOLの巨大な
    EVALUATE/IFツリーは業務ロジックの複雑さが分岐数にそのまま表れるため、C1相当が実質的な
    網羅の指標になりやすかった。一方、`is_valid_set`のように小さく分解された純粋関数では、
    複雑さは分岐の数ではなく入力値の組み合わせ（値域）に移動する。分岐は2〜3個しかないので
    C0/C1は数回の呼び出しでほぼタダで100%になるが、それは境界値（`6-5`等）を試したことには
    ならない。したがって分解された設計では、構造カバレッジは「安全網（実行されていない分岐の
    発見）」に格下げし、同値分割・境界値分析を主軸に据えるべき、という結論に至った
  - `classify_line`のように複数セットの集約結果に依存する部分は、依然として分岐の組み合わせ
    網羅が意味を持つ、という使い分けも整理した

### やったこと（実装）
- `pytest`・`pytest-cov`を導入（`pip install pytest pytest-cov`）
- `tennis_stats/test_tennis_stats.py`を新規作成。上記の整理を踏まえた設計:
  - `is_valid_set`: 妥当な境界のすぐ内側／すぐ外側をペアで確認する境界値分析（`6-4`と`6-5`等）
  - `parse_set_token`: valid/corrected/rejected-format/rejected-valueの同値クラスを網羅
  - `classify_line`: 複数セットにまたがる集約の優先順位（rejectedがcorrectedより優先される
    ことを、順序を入れ替えた2パターンで確認）
  - `input_matches_from_csv`: 既存の`sample_matches.csv`/`sample_matches_dirty.csv`を使った
    回帰確認
- 24件のテストが全てパス
- `pytest --cov`でカバレッジを取得すると47%。未カバー部分は集計関数群（`did_win`等、
  境界値分析が効きにくい単純なロジック）と`input_matches`/`main`（`input()`に依存し
  単純な単体テストでは検証しづらい部分）。「リスクが高く境界値分析が効く部分に絞って
  テストを設計した結果」であり、意図的に低い数字であることを確認した

### 詰まった点・気づき
- カバレッジの数値そのものより、「どこにテストの労力を割くべきかを自分で判断できること」の
  方が重要だと実感した。47%という数字だけ見ると手抜きに見えるが、根拠を説明できれば問題ない、
  という感覚は品質保証の考え方そのもの
- C0/C1/C2/MC-DCという伝統的な基準を「古い」と切り捨てるのではなく、「どんなコードの書き方に
  対して有効な指標か」という前提条件込みで理解し直せたのが収穫だった

### 次回予定
- 集計関数群（`did_win`/`win_rate`等）への具体例ベースのテスト追加を検討
- `input_matches`（対話入力）のテストを`monkeypatch`で`input()`を差し替えて追加するか検討
- 見送っていた「試合単位の整合性チェック」（矛盾したセット数など）への対応検討
- リタイア・サスペンデッド用の`DEF`表記対応の検討
- ダッシュ文字違い・余分な空白など、他の表記ゆれへの補正拡張を検討するかどうか

---

## 2026-08-09（マネジメント視点の振り返り）

### やったこと
- 今日の一連のテスト設計の議論を踏まえ、従来の品質管理（工程ごとのテスト密度・バグ密度が
  水準値範囲か否かの定量評価＋いろんな観点での定性評価、という2段構え）が、AI駆動開発では
  どう変わるべきかを質問
  - 整理: 「定量評価が不要になる」のではなく、「定量評価の対象・分母・粒度が変わる」
    というのが正確な捉え方
  - 従来のKLOC当たりバグ密度・テスト密度が機能していた前提:
    1. コードは人間が書く → コード量が投入労力・複雑さにほぼ比例する
    2. 工程（単体/結合/総合）が明確に区切られ、フェーズごとにレビューゲートがあり、
       過去データから管理図的な水準値を蓄積できる
    3. 長期間・大人数開発で、母数となる過去実績のサンプルが十分にある
  - AI駆動開発ではこの前提が崩れる:
    - コード量と労力の比例関係が崩れる（AIは正しい定型コードを大量に一瞬で書ける。
      今日の`is_valid_set`3行 vs `test_tennis_stats.py`100行超の比較で、リスクが高いのは
      圧倒的に前者、という例で説明）
    - 工程の境目が溶ける（今日のセッション自体が「設計確認→実装→テスト→コミット」を
      数十分のループで回しており、フェーズ単位の水準値蓄積という発想が成立しにくい）
    - 結論: KLOC分母のバグ密度・テスト密度はAI駆動開発ではそのままでは意味を失いやすい
  - 一方、水準値という「マネジメントの発想の型」自体は、分母を変えれば生き残る。候補:
    - 識別した同値クラス・境界値のうち実際にテストされている割合（今日の47%カバレッジの
      逆側の実践がまさにこれ。コード行数ではなく仕様として洗い出したケースを分母にする）
    - 変更（diff）に対するテストの追随率（新しい分岐・ルールを入れたコミットに対応する
      テストが含まれているか）
    - AIが生成したコードのうち、レビューで修正が入った割合（AIの提案をそのまま採用した
      比率＝どこに人間のチェックが効いているかを可視化する新しい密度指標）
  - 定性評価の重みはむしろ増す、という整理も合わせて確認
    - 前回話した「Python初心者としてのレビューの限界」（言語仕様の細部は今の実力では
      見きれない）を踏まえると、人間が引き続き高精度で判断できるのは定性評価の領域
      （境界値の選び方が業務的に妥当か、rejected/correctedの優先順位が仕様として正しいか等）
    - 定量評価がコード量ベースから外れて頼りにくくなる分、相対的に定性評価への依存度は
      上がる、という向きで結論

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 「品質管理の定量評価という発想自体を捨てるべきか」という問いは、実は「何を分母に
  置くかを変える必要がある」という問いに言い換えられる、という気づきが今日一番の収穫
- KLOC・行数という、長年当たり前に使ってきた分母がAI駆動開発では意味を失う一方、
  「仕様として識別したリスク項目（同値クラス・境界値）」を分母にすれば、水準値という
  マネジメントの型自体はそのまま延命できる、という具体的な置き換え案が見えたのは大きい
- 定量評価が後退する分、定性評価（人間にしかできない判断）の重要性が相対的に増す、
  という逆説的な結論は、「AIに任せられる部分が増えるほど、人間の役割は軽くなる」という
  素朴なイメージとは逆で、意外性がありnote記事の題材としても強い

### 次回予定
（上記「2026-08-09（続き）」の次回予定を継続）

---

## 2026-08-11

### やったこと
- 前回終了時点で`pytest`が2件失敗していた原因を調査。`test_tennis_stats.py`が
  サンプルCSVのファイル名を`"sample_matches.csv"`のようにベタ書きの相対パスで指定しており、
  `pytest`を実行するカレントディレクトリ（`learning-python`直下か`tennis_stats`直下か）に
  よって見つかったり見つからなかったりしていた。`pathlib.Path(__file__).parent`で
  テストファイル自身の場所を基準にした絶対パスに直して解消
- 表記ゆれ補正の対象を拡張（次回予定に挙げていた「ダッシュ文字違い・余分な空白」に対応）:
  - ダッシュのバリエーション（全角ハイフン、ENダッシュ、EMダッシュ、マイナス記号、
    日本語入力で紛れやすい長音記号「ー」など）を半角ハイフンに正規化
  - セットスコア内の余分な空白（`"6 - 3"`のようにハイフンの前後に空白が入るケース）を除去
  - どちらも既存の`corrected`扱いの枠組みにそのまま乗る形で実装（新しい判定ロジックの追加は
    不要で、`normalize_score_text`の正規化テーブル・処理を拡張するだけで済んだ）
- `test_tennis_stats.py`にダッシュ表記ゆれ・内部空白のテストを追加（パラメータ化、8件）。
  `sample_matches_dirty.csv`にも該当ケース2行を追加し、動作確認用サンプルにも反映
- 全32件のテストがパス

### 詰まった点・気づき
- 「動かない」の原因が実装ロジックではなく、テストコード内の相対パス（実行時のカレント
  ディレクトリに依存）だった点。COBOLのJCLでのデータセット名解決（`ASSIGN TO`）と対比すると、
  Pythonの相対パスが「どこから実行したか」に左右されるのは感覚が違う、という気づきがあった
- 表記ゆれ補正の拡張が、既存の「正規化して比較し、差分があればcorrected」という設計のおかげで
  新しい分岐を増やさずに済んだ。最初の設計（エンティティの制約と境界対応を分離する発想、
  2026-08-09参照）がここでも効いた形

---

## 2026-08-11（続き: Webスクレイピングと非構造化データ）

### やったこと
- テニス集計プログラムの入力方法（対話入力／CSV）に加え、Webサイト（flashscore.co.jp）から
  スコアを直接取得できないか質問
  - `robots.txt`を実際に取得して確認したところ、多数のクローラー・AIボットを名指しで
    全面ブロックしており、スクレイピング禁止の意思表示が強いサイトだと判明
  - ライブスコアのような「データそのものが収益源」のサイトはスクレイピング不可なことが多い、
    という一般的な勘どころに加え、実際にrobots.txtを確認する、という2段構えのリスク検知の
    仕方を説明。「robots.txt（技術的な意思表示）」と「利用規約(ToS、法的な契約文書)」は
    別物で、robots.txtの厳しさからToSの厳しさを推測しているに過ぎない、という限界も明示
  - 「気づき方は頼もしいが、それでも人間側の確認は必要」という理解に至った
- 練習用に使えるスクレイピング用サンドボックスサイトを探索（同じくrobots.txt確認込み）
  - `scrapethissite.com`: 「The internet's best resource for learning web scraping」と明記、
    `robots.txt`も`/lessons/`と`/faq/`以外は制限なし
  - `books.toscrape.com`: 「We love being scraped!」と明記、robots.txt自体が存在しない
  - `quotes.toscrape.com`: Scrapy公式チュートリアル用に作られたサイト（Zyte社製）
  - いずれも安全に練習できると判断し、`scrapethissite.com`のホッケーチーム成績表
    （`/pages/forms/`）を今回の題材に選定（テニス集計と同じ「表形式のスポーツ統計」で
    構造が近いため）
- `requests`・`beautifulsoup4`を導入し、`scraping_practice/hockey_stats.py`を新規作成
  - `requests.get()`でHTML取得 → `BeautifulSoup`で`tr.team`ごとに`.name`/`.year`/`.wins`/
    `.losses`を抽出、という最小構成
  - 実行前に実際のHTMLをcurlで取得し、class名（`name`/`year`/`wins`/`losses`等）を
    確認してからコードを書いた（推測でセレクタを書かない）
  - 1ページ目（25チーム）の取得に成功。全24ページへの対応は次回持ち越し
  - **未コミット**（`scraping_practice/`はgit未追跡のまま。次回コミットするか要判断）
- 「音声・画像のような非構造化データはどう扱うか、データ分析のインプットになり得るか」を質問
  - 整理: 構造化データ（CSV/DB）はCOBOLのPIC句・コピー本のように事前にスキーマが
    決まっているが、画像・音声は「決まった列」を持たない生の数値の塊（画像はピクセル値の
    行列、音声は波形のサンプル値）
  - 分析に使うには「生データ→特徴抽出→固定長の数値表現（特徴量ベクトル/embedding）→
    分析」という変換ステップを挟むのが基本形。深層学習はこの変換自体もモデルに学習させて
    しまうことが多い、という現代的な補足も追加
  - 非構造化データ自体は扱いにくくても、付随するメタデータ（画像の解像度、音声の長さ等）は
    最初から構造化データであり、そこだけでも分析の足がかりになる、という実務的な視点も共有
  - 「データレイク（生データの保管）」と「データウェアハウス（構造化後の分析用データ）」の
    2層構造という整理が、マネージャ視点で一番腹落ちしたポイント

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 「Webから直接データを取ってきたい」という自然な発想に対して、技術的な可否だけでなく
  「相手の利用規約・robots.txtを先に確認する」というリスク検知プロセスそのものが
  新鮮だった様子。「気づき方の特徴」を尋ねられた際、`robots.txt`（技術的意思表示）と
  利用規約（法的契約）が別物であり、前者から後者を推測しているに過ぎないという限界を
  正直に説明したところ、「頼もしい反面、人間の確認も必要」という結論に自ら到達した
  → AI駆動開発における「AIに任せられる部分」と「人間が最終判断すべき部分」の切り分けが、
    今回はリスク検知という文脈で具体的に立ち現れた回。8/2の権限確認プロンプトの話
    （実行許可と判断基準は別、という整理）と同じ構造がここにも表れている
- 非構造化データの話は、直接のコーディング作業ではなく概念整理の回だったが、
  「データレイク／データウェアハウス」というアーキテクチャの型に落とし込んだことで、
  自治体システムでのデータ管理経験と接続できた様子

### 次回予定
- `scraping_practice/hockey_stats.py`の複数ページ対応（`?page_num=X`）
- `scraping_practice/`をコミットするかどうかの判断
- 公式APIの調査（利用規約を含めて確認する方向で）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-13（複数ページ対応とコミット整理）

### やったこと
- 実装前に`curl`で`?page_num=2`のHTMLを実際に取得し、ページネーションの仕様を確認
  （`page_num=1`〜`24`、1ページ25チーム、24ページ目のみ端数7チーム）。推測でコードを
  書かず、まず実物を見るという前回までの進め方を踏襲
- 終了判定の設計を「固定回数（`range(1, 25)`、COBOLのPERFORM VARYING相当）」か
  「動的検知（空ページで停止、PERFORM UNTIL相当）」かの2択で確認 → 動的検知を選択。
  理由: サイト側のページ数が将来変わっても壊れない設計を優先する判断
- `hockey_stats.py`に`fetch_all_teams()`を追加。`page_num`を1から増やしながら取得し、
  空ページ（`tr.team`が0件）に行き着いたら停止。あわせて`time.sleep(0.5)`で
  リクエスト間隔を空ける処理も追加（練習用サイトでも連続アクセスは避ける、という
  8/11のリスク検知の話を実装に反映した形）
- 実行して24ページ・合計582チーム分の取得を確認（24ページ目は端数7チームで正しく停止）

### 詰まった点・気づき
- Edit操作で`(1ページ目)`のような全角括弧が半角だと思い込んでツール呼び出しが3回
  失敗した。日本語コメント・メッセージの全角/半角混在は、人間が読むときは気にならないが
  機械的な文字列一致（この場合はコード編集ツール）には効いてくる、という地味な落とし穴

### 次回予定
- `scraping_practice/`と本エントリをコミット
- 公式APIの調査（利用規約を含めて確認する方向で）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-15（公式APIの調査と実践）

### やったこと
- 「公式APIの探し方・制約の確認方法・具体的な利用イメージが湧かない」という質問を受け、
  実装前にまず一般論を整理:
  - 探し方: サイトの"API"/"Developers"リンク、なければ「サイト名 API docs」で検索
  - 制約確認: APIドキュメント（技術仕様）と利用規約（法的契約）は別物として両方読む
    → COBOL対比で「他システムとのIF定義書（項目定義＋運用ルール）」と説明
  - 典型的な制約: 認証（APIキー）、レート制限、利用目的の制限、再配布可否、帰属表示
  - JSON形式のレスポンスは「項目名がデータに埋め込まれた自己記述的レコード」で、
    HTMLパース（UNSTRING的な力技）に比べて扱いやすいことも対比で説明
- 具体例として、スポーツ統計系の無料公開API「TheSportsDB」を選定し、実際にドキュメント→
  利用規約の順で読んで制約を確認（`WebFetch`で該当ページを取得）:
  - 無料テスト用キー`123`が公開されており登録不要で試せる
  - レート制限: 無料枠30リクエスト/分、超過時は`429`
  - 無料枠はアプリストア公開不可（有料サブスクリプション必須）、無許可の再配布禁止、
    スクレイピング自体も規約で禁止（＝APIを使わせる代わりに直接スクレイピングは不可）
- `curl`で`search_all_teams.php?l=NHL`・`searchteams.php?t=...`のレスポンス形式を
  先に確認してから、`api_practice/sportsdb_teams.py`を新規作成
  - `hockey_stats.py`と対になる構成（`fetch_teams_by_league()` → `main()`）
  - `requests.get()` → `response.json()`で辞書化するだけで、`BeautifulSoup`によるHTML
    解析が不要になる点を実際のコード量の差で体感
- 実行してNHLチーム一覧を取得、動作確認済み

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 実行してみたところ、NHL（本来32チーム）のはずが**10チームしか返ってこなかった**。
  生のAPIレスポンス自体が10件だったため、コード側のバグではなく無料テスト用キー`123`
  固有の件数制限と判明。この制限はドキュメント・利用規約のどちらにも明記されておらず、
  **実際に動かして初めて発覚する制約**だった
  → 8/11（robots.txtと利用規約の限界）・8/13（設計判断の文脈情報はコードから読めない）
    と同型で、「ドキュメントを読む」だけでは制約の全体像は分からず、動かして初めて
    見える部分が必ず残る、という気づきが4度目に立ち現れた回。COBOL時代の「テスト環境と
    本番環境でマスタデータの件数が違い、テストでは気づけない制約があった」経験と
    重なる、とのコメントあり

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-15 続き（有名APIの比較、GitHubへの初push）

### やったこと
- TheSportsDB以外の有名API（GitHub API、OpenWeatherMap、Google Maps Platform）を
  `WebFetch`でドキュメントを読み比べ、認証方式・レート制限・課金モデルの違いを整理:
  - GitHub API: 未認証60回/時間、認証済み5,000回/時間（登録の重さが軽い）
  - OpenWeatherMap: APIキー登録制、60回/分・100万回/月の無料プラン
  - Google Maps Platform: APIキー取得に**Google Cloudの課金設定（クレジットカード登録）が
    必須**。無料プラン（超過しても止まるだけ）と、無料枠＋従量課金（超過すると自動課金）の
    2種類があるという課金モデルの違いが今回の一番の発見
- GitHub APIの認証なしアクセスを自分のリポジトリで試したいという流れになったが、
  GitHubアカウントを持っているか本人も記憶が曖昧だった
  - ローカルのgit設定・SSH鍵・Windows資格情報マネージャーを確認したが手がかりなし
  - 「認証なしでユーザー存在を確認できる」というGitHub APIの性質をそのまま使い、
    `GET /users/matsu64000`を叩いたところ200 OKでアカウント実在を確認（作成日
    2026-07-11、`public_repos: 0`）。ちょうど今回学んでいる内容そのもので確認できた
- `learning-python`をこのアカウントに初めてpush。`gh` CLI未導入・保存済み認証情報も
  なかったため、リポジトリ作成はブラウザ、pushはGit Credential Manager経由のブラウザ
  ログインで、ユーザー本人に`!`プレフィックスで直接実行してもらう形を取った
  - 空リポジトリのデフォルトブランチが`main`だったため、ローカルの`master`を
    `git push -u origin master:main`で対応付け
- push後、`api_practice/github_repos.py`を新規作成し、`GET /users/{username}/repos`で
  自分のリポジトリ一覧を認証なしで取得。実行結果は`learning-python (public, branch: main)`

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- push直後にAPIで確認したところ、`pushed_at`は更新されていたが`size`フィールドだけ
  しばらく`0`のままだった。contents APIで中身自体は正しく届いていることを確認できたため、
  「サイズ集計だけ非同期のバッチ処理で遅延する」というAPI側の実装都合と判断。
  これも8/15前半と同型の「ドキュメントに書かれていない、動かして初めて分かる挙動」で、
  同じ日に2回続けて同じパターンに遭遇したことになる
- 「アカウントを作った記憶があるが自信がない」という曖昧な状態から、ローカルの手がかり
  （git設定・SSH鍵・資格情報マネージャー）を先に確認し、それでも分からなければ対象の
  外部サービス自身のAPI（今回はGitHubの未認証ユーザー検索）で存在確認するという段取りは、
  自治体システムで「本人が古いアカウントの存在を忘れていた際、まず手元の台帳を確認し、
  それでも不明なら当該システムに直接照会する」という調査の順序と同じ構造。学習中のAPIを
  そのままメタ的に「調査の道具」として使えた回

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-15 続き2（非構造化データ: 画像をPillow/numpyで読む）

### やったこと
- `Pillow`・`numpy`を新規導入
- 進め方の確認: 「Pillowで自作する」という選択が、実際にはClaudeがコードを書いて実行する
  形になったことについて、本人から「自分は何もしていないのでは」という疑問が出た
  → 「自作」は『手元の写真ファイルを使う』との対比で、"画像データをコードで生成する"
    という意味だったと説明。あわせて[[教え方の指針]]に沿い「動くコードを先に見せて
    から解説する」進め方であることを改めて確認し、①コードを読んで理解する路線か、
    ②構造だけ示して値の穴埋めは本人が書く路線かを聞いたところ、①（読解中心）を継続する
    ことで合意
- `image_practice/pixel_basics.py`を新規作成。2行4列・8ピクセル分のRGB値を明示的に
  指定し、`numpy.array(pixels, dtype=uint8)` → `Image.fromarray()`で画像化、
  `sample_image.png`として保存。あわせて`numpy.array(image)`で読み戻し、
  `.shape`・`.dtype`・各ピクセル値を1つずつ表示
  - COBOL対比: `array.shape`の`(2, 4, 3)`は`OCCURS 2 OCCURS 4`のR/G/B(各`PIC 9(3)`)
    に相当する入れ子構造として説明
  - `image.size`が`(4, 2)`（幅×高さ、Pillowの慣習）、`array.shape`が`(2, 4, 3)`
    （高さ×幅×チャンネル、numpyの慣習）で軸の順序が異なる点を、実際に両方printして
    比較することで確認（ドキュメントだけでは体に入らない類の話）
- `image_practice/feature_extraction.py`を新規作成。8/11に整理した「生データ→特徴抽出→
  固定長の数値表現」の流れを`sample_image.png`で実践:
  - グレースケール変換（`image.convert("L")`）で3チャンネル→1チャンネルに圧縮。
    単純平均ではなくPillow標準の輝度計算式（緑を重く、青を軽く見る重み付け）で
    変換されることを、実際の出力値から確認
  - `array.mean(axis=(0,1))`でチャンネルごとの平均値（3要素の固定長ベクトル）を算出。
    COBOL対比でコントロールブレイクの合計行に相当すると説明
  - `array.flatten()`で2次元構造を1次元ベクトル（要素数24）に展開。機械学習モデルへの
    入力形式としてよくある前処理であることも補足
- 生成物`sample_image.png`は`pixel_basics.py`を実行すれば再現できるため、既存の生成物
  除外方針（`.coverage`等と同様）を踏襲し`.gitignore`に追加、コードのみコミット

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 「Pillowで自作する」という選択肢名から、本人はコードを自分の手で書くことを想定していた
  様子。実際にはClaudeが書いて実行するいつもの進め方だったため、選択肢の言葉と実際の作業
  分担の間にズレが生じた。指摘を受けてから役割分担（読解中心か実践中心か）を明示的に
  聞き直したことで解消したが、「AIとの協働で誰が何を『やった』ことになるのか」という、
  AI駆動開発特有の役割認識のズレは、部下への作業指示でも起こりうる（指示の言葉が曖昧だと
  誰が手を動かすか誤解される）ため、記事の題材候補としてメモ

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応
- 非構造化データの続き（音声データを扱ってみる、または画像の特徴量を使った簡単な分析）

---

## 2026-08-16（画像の類似度判定: 平均色→ブロック分割→標準偏差と限界を辿る）

### やったこと
- 前回の予告どおり、非構造化データの続きとして「画像特徴量を使った簡単な分析」を選択。
  ユークリッド距離とコサイン類似度の違いを説明した上で、まずユークリッド距離を採用
- `image_practice/color_similarity.py`を新規作成。`feature_extraction.py`の`average_color`を
  再利用し、自作した8x8ピクセルの画像5枚（red / red_light / blue / checker_red_white
  （1ピクセル単位の市松模様）/ pink（checkerと平均色がほぼ一致する単色））の平均色ベクトル同士の
  ユークリッド距離を計算
  - 結果: `checker_red_white` vs `pink`の距離が0.7とほぼ同一判定。見た目は全く違う画像なのに
    平均色だけでは区別できないことを数値で確認（配置情報が平均という操作で失われるため）
- 改善策として2x2ブロック分割（ブロックごとの平均色を並べた12要素ベクトル）を追加実装。
  併せて`half_red_white`（左半分赤・右半分白。checkerと平均色は同じだが模様がブロック境界と
  一致する粗さ）を新設して比較
  - 結果: `half_red_white` vs `pink`は360.6まで引き離され正しく区別できたが、
    `checker_red_white` vs `pink`は1.4のまま誤判定が継続。模様の周期がブロックの粒度より
    細かいと、ブロックをいくら細かくしても平均が均されてしまうため
- 標準偏差（`array.std(axis=(0,1))`）を平均色に追加した6要素ベクトルも試した
  - 結果: `checker_red_white` vs `pink`は180.3まで引き離され正しく区別できるようになった。
    一方`checker_red_white` vs `half_red_white`は0.0のまま誤判定（両方とも赤白ちょうど半々の
    画素構成のため、平均も標準偏差も同一値になる）

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- **本人から「標本化定理」との連想が出た回**。ブロック分割による低解像度化はローパス
  フィルタ＋ダウンサンプリングと数学的に同じであり、`checker_red_white`（1ピクセル周期＝
  ナイキスト周波数そのもの）はブロックの粒度をいくら変えても復元不能、というのが帯域制限
  信号の理屈とぴったり一致することを整理。本人からは「学生時代、帯域制限された信号は離散
  サンプルから完全復元できるが、帯域制限のない信号はできないという研究をしていた」という
  コメントがあり、30年以上前の専門（信号処理、シャノン/シュワルツ等の定理・公式）が
  Python学習の文脈で自然に接続した回。COBOL/FORTRAN対比とは違う、**もう一段古い専門知識**が
  新しい技術理解の橋渡しになった初めての例
- さらに本人から「ランダムサンプリングを繰り返せば、いずれ同じ平均に収束するはずだから、
  標準偏差のような手法で別画像と判定できるのでは」という仮説が自発的に出た。実際に
  「大数の法則で平均は収束するが、値のばらつき（分散・標準偏差）という2次のモーメントは
  区別に使える」という統計的に正しい筋道であり、Claude側の説明を待たずに本人が自力で
  結論にたどり着いた回。実装して検証したところ仮説通り`checker_red_white` vs `pink`は
  区別できるようになった一方、`checker_red_white` vs `half_red_white`（配置だけが違い、
  値の分布は完全に同一）は標準偏差でも区別できないという新しい限界にも遭遇し、
  「平均（1次）→標準偏差（2次）→それでも配置情報だけはどちらにも残らない」という
  きれいな構造の理解に至った
- 全体を通じて「特徴量は何かを捨てて要約する操作であり、何を残し何を捨てるかにトレード
  オフがある」という非構造化データ処理の勘所を、数値と画像を実際に見比べながら体得できた回

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応
- 非構造化データの続き（音声データを扱ってみる、または位置情報を残す別の特徴抽出
  （エッジ検出など）を試す）

---

## 2026-08-16 続き（エッジ検出で「配置だけが違う」問題に決着をつける）

### やったこと
- 標準偏差でも`checker_red_white` vs `half_red_white`（値の分布は同一、配置だけが違う）を
  区別できなかった件を受けて、エッジ検出（隣接ピクセルの差分）の考え方を説明。
  「隣り合うピクセルの変化率を見る」という発想は、本人が最初の方で自発的に述べていた
  「隣り合ったピクセルの組み合わせを評価する」という直感と同じものだったことを確認
- `image_practice/color_similarity.py`に`edge_energy`関数を追加。グレースケール変換
  （`feature_extraction.py`の`to_grayscale_array`を再利用）した上で、縦横それぞれの
  隣接差分（`np.diff`）の絶対値を合計する、最も単純な形のエッジ検出を実装
  （Sobel/Laplacianのような重み付きフィルタの説明はしたが、実装はまず単純な隣接差分版で足りるか確認する方針）
- 平均色+標準偏差+エッジ強度の7要素ベクトルで、既存の4画像+checker/half_red_white/pinkの
  距離を再計算
  - 結果: `checker_red_white`のエッジ強度=20048.0、`half_red_white`=1432.0、`pink`=0.0と
    模様の細かさどおりの値になった。これにより`checker_red_white` vs `half_red_white`の
    距離が0.0(誤判定)→18616.0まで一気に引き離され、平均・標準偏差では区別できなかった
    「配置だけが違う」ケースをようやく区別できた

### 詰まった点・気づき
- 「全体平均→ブロック分割→標準偏差→エッジ強度」の4段階を通じて、それぞれの特徴量が
  「何を捉え、何を捨てているか」がきれいに対比できる表にまとまった（全体平均: 色味の代表値、
  ブロック分割: 粗い配置、標準偏差: 値の散らばり、エッジ強度: 隣接ピクセルの変化＝配置情報）。
  checker/half_red_white/pinkという3枚の作為的なテスト画像だけで、非構造化データの特徴抽出の
  設計思想（何を残すかは目的次第、万能な特徴量は無い）をひととおり体験できた一連の流れになった
- 本人の「隣り合ったピクセルの組み合わせを評価する」という最初の発言が、遠回りせず
  エッジ検出の本質を言い当てていた、という気づきも本人と共有

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応
- 非構造化データの続き（音声データを扱ってみる。または今回の4手法をSobelフィルタなど
  より実務的なエッジ検出に発展させる）

---

## 2026-08-16 続き2（音声データ入門: FFTで周波数分解、倍音とエンベロープで音色を作る）

### やったこと
- 画像編（画素という定量化）に続き、音声はどう定量化されるかという質問をきっかけに開始。
  音声=時間軸上に並んだ振幅値(1次元の時系列)であり、標本化定理(本人の学生時代の専門)が
  サンプリングの段階で直接効いてくること、画像の空間周波数(エッジ検出の話)と音声の時間周波数が
  同じフーリエ解析の土台にあることを整理してから実装
- `audio_practice/fft_basics.py`を新規作成。`numpy.fft.rfft`でFFT、`wave`モジュール(追加
  ライブラリ不要)で16bit PCM WAV保存。440Hz単体と440Hz+1000Hz混合の2種類の波形を生成し、
  時間領域(波形)と周波数領域(スペクトル)をmatplotlibで並べて可視化
  - 結果: 単体は440Hzに1本、混合は440Hzと1000Hzの2本のピークがきれいに分離して検出された。
    時間領域の波形だけを見ても何が混ざっているか分からないが、周波数領域に変換すると
    構成要素がはっきり見える、というフーリエ変換の効能を実際に確認
  - 新規導入: `matplotlib`。Windowsで日本語ラベルが文字化けする問題に新たに遭遇。
    `MS Gothic`(ビットマップフォント)を指定したところ文字が一切描画されず(警告も出ないまま
    無言で消える)、`Yu Gothic`(アウトラインフォント)に変更して解決。フォント種別
    (ビットマップ/アウトライン)とレンダラーの相性という、また一段深い「動かして初めて
    分かる」トラブルシューティングになった
- 続けて、「ピアノのラとギターのラはなぜ違って聞こえるか」という質問から、音色(timbre)の
  正体（倍音の構成比率＋音量の時間変化=エンベロープ）を整理。`audio_practice/timbre_basics.py`
  を新規作成し、`fft_basics.py`の関数を再利用して「ピアノ風」（倍音を急速減衰させる
  エンベロープ、6次倍音まで急激に減衰する構成）と「オルガン風」（一定音量が持続する
  エンベロープ、倍音がなだらかに残る構成）を合成
  - 結果: エンベロープ（左列グラフ）だけでも三角形型(ピアノ風)と四角型(オルガン風)で一目瞭然。
    倍音の相対的な高さの配分の違いもスペクトルで確認
  - **おまけの発見**: ピアノ風は急速に減衰するため、理想的な1本の周波数として存在できず、
    検出ピークが440Hz付近で2本に割れる(スペクトル漏れ)現象に遭遇。不確定性原理と同じ
    数学的背景（時間的に変化する信号ほど周波数軸でピークがぼやける）で、持続音（オルガン風）は
    きれいな1本のピークだった対比が、スペクトログラムの必要性（前回説明済み）を裏付ける
    形で実証された

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 画像編の「配置情報（空間方向）」と、音声編の「時間変化（時間方向）」が、同じフーリエ解析・
  帯域制限という数学的土台の上で対応関係にあることを、実装・実行を通じて一貫して確認できた
  一連の流れになった。8/16の一日だけで「画像の限界→音声への一般化→本物の楽器の複雑さ」まで
  理屈と実験が繋がった回
- `MS Gothic`での無言の描画失敗は、8/15の「ドキュメントに書かれていない、動かして初めて
  分かる制約」という縦糸（[[project-learning-python]]参照）にさらに1件追加される事例

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応
- 非構造化データの続き（スペクトログラム(STFT)を実際に実装して時間変化を可視化する。
  または画像編のSobelフィルタなど、より実務的な手法への発展）

## 2026-08-22（音声データ入門: STFTでスペクトログラムを実装）
- 前回(8/16)の「一括FFTでは時刻情報が失われる」という予告を受け、`audio_practice/stft_basics.py`
  を新規作成。窓(window)をずらしながらFFTを繰り返しかけるSTFT(短時間フーリエ変換)を、
  ライブラリに頼らず`np.hanning`+`np.fft.rfft`の手組みで実装（`compute_stft(waveform,
  window_size, hop_size)`）
- 題材は2つ: (1) 440Hz→880Hzに0.5秒で切り替わる信号、(2) 200Hz→2000Hzへ1秒かけて連続的に
  上昇するチャープ信号。どちらも一括FFT(`fft_basics.compute_spectrum`を再利用)では
  「2つの周波数が存在する」「幅広い周波数帯にエネルギーがある」ことしか分からず、
  「いつ」切り替わった/上昇したかは波形を見ないと分からない。STFTのスペクトログラム
  (`imshow`で時間×周波数×強度をヒートマップ表示)にすると、切り替わりの瞬間や
  右肩上がりの軌跡がそのまま画で見える
- **image_practice/color_similarity.pyとの構造的対応に気づいた回**: 「平均色だけでは
  配置情報(空間方向)が失われる」(8/16)と「一括FFTだけでは時刻情報(時間方向)が失われる」が
  同じ形の問題だと事前に整理してから実装。「全体を一括で要約すると、要約に使わなかった軸が
  消える」という一般化ができた
- **窓サイズのトレードオフを実験で可視化**: 同じチャープ信号を窓サイズ256サンプル(5.8ms)と
  4096サンプル(92.9ms)でSTFTし、`stft_window_tradeoff.png`として並べて保存。短い窓は
  時間方向の変化を細かく追えるが周波数方向がぼやけ、長い窓はその逆というトレードオフが
  グラフの見た目の違いとして確認できた。これはtimbre_basics.pyで遭遇した「ピアノ風の
  急減衰音はピークが2本に割れる」現象(8/16)と同じ不確定性原理が、今回は「窓サイズ」という
  自分で動かせるパラメータとして明示的に扱えるようになった、という発展
  - 短い窓(256)側のスペクトログラムに、対角線に沿って周期的な縞模様(コムフィルタ的な
    アーティファクト)が出た。窓長に対してチャープの周波数変化が相対的に速いために生じる
    スペクトル漏れの一種と考えられ、意図した「ぼやける」という直感的な見た目とは少し違う
    形で現れた。窓サイズを変えるとFFTの結果の見た目が単純に鮮明/不鮮明になるだけでなく、
    こうした別種のアーティファクトも一緒に出てくる、という「動かして初めて分かる」系の
    気づきが今回も1件追加された（[[project-learning-python]]参照の縦糸）

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 8/16の画像編（配置情報の消失）と今回の音声編（時刻情報の消失）が、実装する前から
  「同じ構造のはず」と見通しを立てて臨めた回。これまでは実装・実行して初めて気づく
  パターンが多かったが、今回は経験の蓄積が先読みとして効いた。「過去の類似パターンを
  抽象化して次に持ち込む」という、経験を積んだ実務者が新しい技術領域でも通用する
  推論の型として、記事の題材になりそう
- 窓サイズのトレードオフは「理屈通りに実装すれば理屈通りの結果になる」典型例だったが、
  短い窓での縞模様アーティファクトは理屈の外側（数値計算の副作用）から出てきたもので、
  「モデル通りの結果」と「実装・実行して初めて出る想定外の副作用」の両方が同じ実験の中に
  同居した回

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応
- 画像編のSobelフィルタなど、より実務的な手法への発展
- 余裕があれば、短い窓で出た縞模様アーティファクトの原因をもう少し掘り下げる（窓関数の種類を
  変えると変化するか、など）

## 2026-08-22 続き（新テーマ: 個人住民税額の試算プログラム）
- 「非構造化データはまた掘り下げたい」としつつ、全く別の新テーマとして「個人住民税額を、
  公開ドキュメント（法律・例規・標準仕様書）を参照して計算できるか」という質問から開始。
  自治体システム開発30年の実務経験と直結するテーマ
- 帳票作成は対象外とし、「収入・扶養情報などのインプットから税額を計算するロジックのみ」に
  絞ることを本人から明示。実装前にAskUserQuestionで3点を確認:
  対象年度=令和7年度(最新)、スコープ=所得割+均等割(住民税額全体)、控除範囲=主要控除一通り
  （基礎控除・給与所得控除・社会保険料控除・扶養控除(人的)）
- **一次資料の調査(WebSearch/WebFetch)を実装前に実施**: 総務省の説明資料に加え、
  春日市・鎌ケ谷市・狛江市など複数の自治体の「令和7年度個人住民税」公式ページを直接参照し、
  税率・均等割内訳・基礎控除の逓減表・給与所得控除の速算表・扶養控除区分別金額・調整控除の
  人的控除差額表・非課税限度額の算式を確認
  - **気づき**: 前回までの「探す→動かす→初めて分かる制約」という順序と異なり、実装前の
    資料調査の段階で既に「自治体ごとの差異」が見つかった。春日市（福岡県）の県民税均等割が
    1,500円（森林環境税相当500円を独自上乗せ）で、鎌ケ谷市（千葉県）の標準額1,000円と
    異なっていた。「全国一律の標準税率」と「自治体条例による上乗せ（超過課税）」の違いが、
    法令を読むだけでなく複数の自治体の実例を突き合わせて初めて可視化された
  - もう1点、「令和7年度」は令和6年中（前年）の所得に課税する前年所得課税である点も、
    自治体ページの記載で明示的に確認（令和7年度税制改正の基礎控除等の引き上げは
    令和8年度住民税から適用される話であり、令和7年度自体には影響しない、という
    年度のタイムラグに注意が必要だった）
- `resident_tax_practice/individual_resident_tax.py`を新規作成。`Dependent`/`TaxpayerInput`の
  dataclassを入力とし、`calc_salary_deduction`（給与所得控除の速算式）、`calc_basic_deduction`
  （基礎控除の逓減）、`classify_dependent`（年齢と同居老親等の情報から扶養区分をEVALUATE的に
  判定）、`calc_adjustment_reduction`（調整控除）、`calculate_resident_tax`（全体を統合し
  内訳を辞書で返す）という構成で実装。COBOLの複数の速算表・区分判定に相当する処理を
  それぞれ独立した関数に分けた
- **スコープ外事項を冒頭にコメントで明記**（tennis_stats.pyの3段階モデル文書化と同じ方針）:
  給与所得のみ（事業所得等は非対応）、配偶者控除等は非対応、非課税限度額の判定は非対応、
  自治体独自の超過課税は非対応（標準額のみ使用）、給与所得控除の660万円未満区間は
  正式な参照表（所得税法別表第5）ではなく速算式で近似（僅かな差異があり得る）
- 2件のサンプル（独身・給与収入500万円/扶養2人・給与収入450万円）で実行確認。
  独身ケースは調整控除が課税標準額の高さにより完全にゼロになる（人的控除差額5万円 <
  課税標準額-200万円の超過分）という、境界条件が実際に発火するケースになった
- コミット・push: 未実施（次回以降の判断）

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- これまでの5〜6回（8/2〜8/16）は一貫して「実装・実行して初めて制約に気づく」パターン
  だったが、今回は「実装前の一次資料調査の段階」で自治体差異という制約に気づけた。
  対象領域が本人の専門（自治体システム、税務）に近いほど、"読むべき一次資料が何か"
  "どこに差異が出やすいか"の勘所が本人の中に既にあり、調査の質そのものが上がる
  という好例になった。「AIが公開資料を集めて要約する」層と、「その資料のどこが
  実務上の落とし穴になりやすいかを見抜く」層が分離しており、後者は専門知識がある
  ほど強く効くという、8/13・8/15の縦糸のさらに濃い版
- 制約事項をコード冒頭に列挙する書き方（tennis_stats.pyの踏襲）が、法令ベースの
  計算ロジックでも同じように機能した。「どこまでを実装したか」を監査可能な形で
  残すという発想が、ドメインが変わっても再利用できる設計パターンだと確認できた

### 次回予定
- 個人住民税: 非課税限度額の判定ロジックの追加を検討
- 個人住民税: pytestでの境界値テスト追加（基礎控除の逓減境界、扶養区分の年齢境界など）を検討
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充

---

## 2026-09-05（個人住民税: 非課税限度額の判定ロジック追加）

### やったこと
- 前回(8/22)の「次回予定」から、非課税限度額の判定ロジックを先に着手（境界値テストは見送り）
- 実装前に一次資料を再確認: 練馬区の令和7年度住民税ページをWebFetchし、
  均等割・所得割それぞれの非課税限度額の算式（35万円×人数+10万円+21万円/32万円、
  扶養なしは単純に45万円）を確認。所得割の限度額(32万円加算)が均等割(21万円加算)より
  必ず高いため、「両方非課税」「所得割のみ非課税(均等割は課税)」「課税」の3区分になる
  構造だと判明
- **気づき（設計判断）**: 非課税限度額の判定に使う「人数」は、扶養控除(所得控除)の対象人数
  （`classify_dependent`が16歳未満を除外する既存ロジック）とは別ルールで、
  **16歳未満の年少扶養親族も人数に含める**必要があるとWebSearchで判明。同じ`Dependent`
  データが、控除計算では除外され、非課税判定では算入されるという、参照する処理によって
  適用ルールが変わる例。COBOLで言えば「同じマスタ項目でも、呼び出すサブルーチンによって
  判定基準が違う」ケースに相当すると対比して腹落ちした様子
- `calc_exemption_thresholds`（人数から両限度額を算出）、`judge_exemption_status`
  （総所得金額等と比較して3区分を判定）を追加。`calculate_resident_tax`内で判定結果に
  応じて所得割・均等割(森林環境税含む)を非課税に上書きする形で統合
- サンプルを2件→4件に拡張。「両方非課税」「所得割のみ非課税」がちょうど発火する
  境界値（給与収入100万円・扶養なしで総所得金額450,000円＝非課税限度額ちょうど、
  給与収入170万円・15歳の扶養親族1人で総所得金額1,120,000円＝所得割限度額ちょうど）を
  意図的に選び、境界条件が実際に発火することを確認。後者では扶養控除が0円のまま
  非課税判定の人数だけ1人になる、という上記の別ルールも数値で可視化できた
- コミット・push: 未pushの2コミット(STFT・住民税初回)とあわせて実施予定

### 次回予定
- 個人住民税: pytestでの境界値テスト追加（基礎控除の逓減境界、扶養区分の年齢境界、
  今回の非課税限度額の境界を含む）
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充

---

## 2026-09-05 続き（個人住民税: 境界値テストを追加）

### やったこと
- 前回予定の「境界値テスト追加」に着手。実装前に「境界値をどう決めているか」を
  本人から質問されたため、tennis_stats(`is_valid_set`)と同じ考え方であることを
  改めて言語化: コード中の比較演算子(`<=`等)がある箇所を境界とみなし、その
  すぐ内側・すぐ外側をペアでテストする(間の値を増やしても検出力は上がらない)
- 境界には性質が異なる2種類があると整理: ①コード中に定数がそのまま書いてある境界
  （`calc_basic_deduction`の24,000,000円等、`classify_dependent`の16/19/23/70歳）は
  条件式の数値をそのままペアにできる。②値自体が計算式から求まる境界
  （前回追加した`calc_exemption_thresholds`/`judge_exemption_status`）は、
  まず閾値を計算してからその値±1をペアにする必要がある、という違い
- `resident_tax_practice/test_individual_resident_tax.py`を新規作成。
  `calc_basic_deduction`(逓減3境界)、`classify_dependent`(年齢4境界+同居老親等の
  同値クラス)、`calc_exemption_thresholds`/`judge_exemption_status`
  (扶養0人は2つの限度額が一致し「所得割のみ非課税」の帯が消えるという構造上の
  特殊ケースを含む)で計23件、全てパス
- `calc_salary_deduction`(給与所得控除の5段階速算表)と`calc_adjustment_reduction`
  (調整控除の2,000,000円境界)も同じ構造の境界を持つが、今回は範囲外として次回以降に
  持ち越し

### 次回予定
- 個人住民税: `calc_salary_deduction`・`calc_adjustment_reduction`の境界値テスト追加
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充

---

## 2026-09-05 続き2（概念整理: 多要素判定のカバレッジ、コードなし）

### やったこと
- 境界値テスト追加後、本人から「複数(多数)の要素を使って判定するケースで、直交条件を
  考慮しつつC0/C1/C2網羅をどう達成するか(本当に全組み合わせにするのか、代表値を取る
  考え方があるのか)」という質問。実装はせず、概念整理のみで区切ることに合意
- 回答の要点:
  1. 全組み合わせ(2^N、複合条件網羅=MCC)は要素数が増えると現実的に破綻するため、
     基本的には行わない
  2. **組み合わせ爆発が起きるかどうかは条件の構造次第**という区別が核心。入れ子の
     if/elif(COBOLのネストしたEVALUATE相当)は分岐ごとに空間が分割されるため
     テスト数は足し算で済むが、独立した要素を1つのifの中でAND/ORで束ねた複合条件は
     掛け算(組み合わせ)になる。`calc_adjustment_reduction`(total_income/taxable_income/
     diff_totalの3要素が絡むが入れ子構造)を例に、2×2×3=12ではなく実質1+3+3=7ケースで
     足りることを具体的に示した
  3. 複合条件が実際に出てきた場合の現実的な選択肢を3段階で提示:
     ①決定表で整理し優先順位・don't careで削減(COBOL時代の決定表と同じ発想。
     tennis_statsの`classify_line`でrejected優先を確認した8/9のテストと同型)
     ②それでも大きいならペアワイズ/直交表(全組み合わせでなく「任意の2要素の組み合わせ」
     は必ず1回登場させる設計。実務のバグの多くが1〜2要素の相互作用で説明できるという
     経験則が根拠)
     ③各条件が本当に結果に効いているかを保証したいならMC/DC(改修条件判定網羅)。
     航空機ソフトウェア認証(DO-178B)がこれを採用しており、安全性が最も求められる
     領域でも全組み合わせは要求されていないという実例
  4. 現状のコードには真に独立な要素を束ねた複合条件がまだ無いため、これは
     「概念として押さえておく」段階と位置づけ。複合条件の題材が出てきたら、
     決定表→ペアワイズ→MC/DCを実際に手を動かして比較する提案をしたが、
     今回は見送り
- **気づき（マネージャ視点、note記事の核候補）**: 8/9に整理した「C0/C1/C2/MC-DCは
  小さく分解された純粋関数では過剰で同値分割・境界値分析を主軸にすべき」という結論の
  "続き"にあたる質問が、境界値テストを実際に書いた直後というタイミングで自然に出てきた。
  「小さい関数の中の話」と「複数要素が絡む判定の話」を区別する感覚は、COBOLの決定表を
  業務ルール定義に使ってきた実務経験と、質問の抽象度そのものに表れている。技術書を
  読むだけでなく手を動かした直後だからこそ出てくる、一段深い設計原則への問い

### 次回予定
- 個人住民税: `calc_salary_deduction`・`calc_adjustment_reduction`の境界値テスト追加
- 複合条件(独立した要素をAND/ORで束ねたルール)の題材が出てきたら、決定表→ペアワイズ→
  MC/DCを実際に比較してみる
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充

---

## 2026-09-06（境界値テスト続き: calc_salary_deduction・calc_adjustment_reduction）

### やったこと
- 前回の積み残しだった2関数の境界値テストを追加。計20件、既存23件と合わせて43件全てパス
- `calc_salary_deduction`(給与所得控除の5段階速算表): 4箇所の境界を実装前に手計算したところ、
  境界をまたいでも値が連続する設計になっていることに気づいた(例: 1,625,000円も
  1,625,001円もどちらも控除額550,000円)。速算表は元々「階段状に不連続な控除額」を避けて
  滑らかにつながるよう設計されているという実務知識が、テスト設計時に裏付けとして出てきた
- `calc_adjustment_reduction`(調整控除): コード中に定数として書かれた境界(200万円・
  2,500万円)に加えて、`min(diff_total, taxable_income)`が効き始める境界と
  `max(..., 0)`で0に張り付く境界という、**値同士の大小関係や計算結果から決まり
  コード上には定数として現れない境界**が2種類あることを整理。前回(9/5)の「値自体が
  計算式から求まる境界は、まず閾値を計算してからその値±1をペアにする」という原則の
  延長線上の実践
- **躓き（自己レビューで発覚）**: `max(...,0)`境界のテストを最初
  `(500_000, 500_001, 500_003)`という3点で組んだが、実行前に手計算し直したところ
  500,001円や500,003円は`base`が1〜3という小さい値になり、その後の`*3//100`
  `*2//100`という整数除算で丸められて結果が結局どちらもゼロになり、
  「floorが効いている場合」と「floorが外れて計算されている場合」を区別できない
  無意味なテストになっていたと気づいた。境界のすぐ隣の値だけを機械的にペアにするのでは
  不十分で、**その先の変換(今回は円単位から税額への整数除算)を経た後も観測可能な差が
  出るか**まで確認する必要があるという教訓。`534,000`という、floorが外れた後に
  十分な大きさを持つ値に差し替えて解決
- **気づき（マネージャ視点）**: 境界値分析は「境界のすぐ内側/外側の値を選ぶ」ところで
  終わりではなく、「その値が最終的な出力の差として本当に観測できるか」まで確認しないと
  テストとして機能しないことを、机上ではなく実際に手を動かして初めて実感した回。
  COBOLの数値項目でも、PICTURE句の桁数やROUNDED指定によって微小な差が丸めで消える
  経験は何度もあったはずだが、今回は「境界値ペアを作る」という作業に集中するあまり、
  一度は見落としていた。設計原則を知っていることと、それを適用する際に手が滑らないことは
  別、という具体例

### 次回予定
- 複合条件(独立した要素をAND/ORで束ねたルール)の題材が出てきたら、決定表→ペアワイズ→
  MC/DCを実際に比較してみる
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充

---

## 2026-09-06 続き（複合条件の実践: 決定表→ペアワイズ→MC/DCを実際に比較）

### やったこと
- 9/5の概念整理の"続き"として、複合条件の実践題材を選定。既存の住民税プログラムには
  未実装だった**寡婦控除・ひとり親控除**の判定ロジックを選んだ（性別・婚姻状況(離婚/
  死別/未婚)・子の有無・他の扶養親族の有無・所得500万円以下・事実婚の有無という
  6要素が絡む、実務でも決定表なしには整理しづらいことで知られる規定）
- `individual_resident_tax.py`に`judge_widow_or_single_parent_deduction`を追加。
  既存の`calculate_resident_tax`にはまだ統合しない独立関数として実装(合意の上)。
  設計のポイント: 生死不明は死別にまとめて簡略化、未婚のひとり親も対象になる
  (令和2年度改正の趣旨)ため`has_dependent_child`の判定は婚姻状況を問わない
- まず動作確認用の代表ケース(11件)で正しさを確認した後、その場で決定表方式に
  差し替えて本題に入った
- **決定表**: 早期リターンの判定順序をそのまま行にし、don't careで削減。
  6要素の全組み合わせ96通り(2×3×2×2×2×2)が、8行に正確に削減できることを
  該当組合せ数の合計(48+24+12+6+2+1+1+2=96)で検証。各行の代表値は
  「don't careの部分にあえて別の結果になりそうな値を選ぶ」ことで、優先順位が
  本当に機能しているかまで確認した(8/9のテニス集計のrejected優先テストと同型)
- **ペアワイズ**: 決定表と違いロジックの構造を使わず、「どの2要素の値の組み合わせも
  最低1回登場させる」機械的基準だけで組む。手作業では抜け漏れが起きるため、
  貪欲法(まだ被覆していないペアを一番多くカバーする組み合わせを選び続ける)で
  生成するスクリプト`pairwise_gen.py`を新規作成。70ペアを8行で被覆(理論上の下限は
  最大レベル数同士の積=3×2=6行だが、貪欲法は最小性を保証しないため8行になった)。
  生成結果をコードで検証(全ペアを被覆できているかをassertで確認)してからテスト化
- **MC/DC**: 「その条件1つだけを変えたら結果が変わる」ペアを5つの決定点それぞれに
  ついて作成。D1〜D4は単一条件の決定なので決定表の意地悪な代表値とほぼ同じものに
  帰着したが、D5(`is_widow = (marital_status=="widowed") or (marital_status=="divorced"
  and has_other_relative)`)は本当の複合条件。3条件(A=is_widowed, B=is_divorced,
  C=has_other_relative)のはずが、AとBが同じmarital_status(3値)から導かれ両立しない
  ため、理論上の最小N+1=4行では足りず**5行必要**になった。行の使い回しにより
  全体では9行で完了
- 3手法とも実装・pytest化して全件パス確認(43件→68件)

### 気づき（本題: 3手法の質的な違い、note記事の核候補）
- 件数はほぼ同じ(決定表8/ペアワイズ8/MC-DC9)だったが、根拠の質が全く違うことが
  実際に手を動かして初めて実感できた。決定表とMC/DCは「ロジックを読んで理解する
  必要がある」代わりに「なぜこの件数で十分か」を説明できる。ペアワイズは
  ロジック理解が実質不要な代わりに、その説明力を持たない(かつ理論的最小値との
  差が大きくなりやすい)。COBOLの決定表による業務ルール定義の経験からすると、
  「読めば説明できるが読む手間がかかる」方式と「読まなくても機械的に組めるが
  説明できない」方式の対比として整理できた
- **AとBの両立不可能性がMC/DCの理論値を崩す**という発見が一番の収穫。
  「1つの列挙値(marital_status)を複数の真偽条件に分解する」というモデル化自体が、
  MC/DC理論が前提とする「条件は独立に真偽を選べる」を暗黙に破っており、
  理論上の最小行数を割り込めなくなる。技術書の一般論(N+1行)をそのまま鵜呑みに
  せず、実際の条件の依存関係を見て初めて分かる制約
- 単一条件の決定点では、決定表の「意地悪な代表値」とMC/DCの「単独効果を示す
  ペア」が実質同じものに帰着した。3手法の違いが本当に現れるのは複合条件の
  部分だけ、という構造が具体的に見えた

### 次回予定
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充
- 余裕があれば、寡婦控除・ひとり親控除を`calculate_resident_tax`に統合するか検討

---

## 2026-09-06 続き2（新しい題材: 三角形の面積・種類判定）

### やったこと
- 住民税とは別枠の新テーマとして、テスト技法の解説でよく使われる古典的な例題
  「3点(x,y)から三角形の面積・種類を判定するプログラム」を選定。新規
  `triangle_practice/`フォルダを作成
- 実装前に3点確認: 座標の型(整数のみ、float非対応)、データ構造(`Point`
  dataclassを3個の`list`で渡す。COBOLのOCCURS 3配列に近いイメージ)、
  不正入力の扱い(型/個数エラーは例外、同一直線上=退化三角形は数学的に正当な
  入力として判定結果(status)で返す)
- `triangle_area.py`に`calc_triangle_area(points)`を実装。同一直線上の判定は
  傾き(slope)の一致ではなく外積(シューレース公式の2倍)で行う。傾き方式は
  垂直な辺でゼロ除算になる弱点があるため採用しなかった。整数計算に閉じているため
  同一直線上判定に浮動小数点誤差が入らない
- **躓きやすいポイントの確認**: `bool`は`int`のサブクラスのため
  `isinstance(value, int)`だけでは`True`/`False`をすり抜けてしまう
  (`isinstance(value, bool)`も明示的にチェックする必要がある)。動的型付け特有の
  落とし穴として、テストケース(`Point(1, True)`)にも組み込んだ
- **気づき（設計判断）**: 座標を整数のみで実装したことで、3点から辺の長さを
  導出する今回の設計では**三角不等式違反(古典的なMyersの三角形問題の目玉の一つ)が
  幾何学的に起こり得ない**と判明。3点はどう置いても三角不等式を満たすか、
  同一直線上(境界値)になるかのどちらかで、後者は既にdegenerate判定で捕捉済み。
  「3辺の長さを直接入力として受け取る」古典版と「点から辺を導出する」今回の版で、
  検証すべき不正系が構造的に変わるという発見

### やったこと（続き: 三角形の種類判定）
- `classify_triangle(points)`を追加。辺の等しさ(edge_type: 正三角形/二等辺/不等辺)と
  角度(angle_type: 直角/鋭角/鈍角)の独立した2軸で分類する複合条件の題材として設計
  (実装前にAskUserQuestionで軸の範囲と出力形式を確認)
- 辺の長さの2乗のまま(平方根を取らずに)比較: 等しさの判定は平方が単調増加なので
  問題なく、角度の判定は余弦定理より最大辺の2乗と残り2辺の2乗の和を比較するだけで
  平方根なしに直角/鋭角/鈍角を判定できる。整数座標のため誤差の心配が一切ない
- **最大の気づき（到達不能な分岐）**: 整数座標に限定したことで、`edge_type=
  "equilateral"(正三角形)は理論上決して出現しないと判明。格子点(整数座標)上の
  正三角形は、面積がシューレース公式より必ず有理数になる一方、正三角形の面積公式
  (√3/4)×辺長²は辺長²が正の整数である限り無理数になり両立しない(証明)。
  分類ロジック自体は一般形(3辺の2乗をsetで比較するだけ)で書くのが自然なので
  equilateralの分岐はコードに残したが、この入力ドメインでは構造的にテストで
  到達できない。9/5に整理した「網羅率の限界」の、非常に具体的な実例が
  実装の過程で自然に出てきた
- 証明だけで終わらせず、-5〜5の範囲で3点の全組み合わせ(約28万通り)を実際に
  総当たりしてequilateralが一度も出現しないことをpytestで確認(8/15〜のpairwise
  検証スクリプトと同じ「主張をコードで検証する」姿勢)
- 到達可能な2×3=6通り(二等辺/不等辺 × 直角/鋭角/鈍角)を全てテストでカバー。
  面積計算とあわせて計19件、全てパス

### 気づき（マネージャ視点、note記事の核候補）
- 今回は1つのセッションの中で「入力の設計判断(整数座標)が、後続の検証項目の
  性質を構造的に変える」という発見が2回連続で起きた(三角不等式違反が消える／
  正三角形が到達不能になる)。どちらも実装前の設計確認の段階では気づけず、
  実装・実行して初めて(あるいは証明を考えて初めて)判明した。COBOL時代の
  「入力の型・精度をどう決めるかが、後工程のテスト設計そのものを左右する」
  経験と地続きの教訓で、設計判断は独立した意思決定ではなく、後工程の検証範囲を
  規定してしまうという感覚が、具体例を通じてまた1つ積み上がった

### 次回予定
- （継続、以前からの積み残し）TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充、寡婦控除・ひとり親控除の`calculate_resident_tax`統合
- 余裕があれば、triangle_practiceのclassify_triangleで決定表/ペアワイズ/MC-DCの
  比較をもう一度やってみる(到達不能なequilateral分岐がある状態でMC/DCがどう
  扱われるか、という新しい切り口が面白そう)

---

## 2026-09-13（三角形の判定でテスト技法を再実践: 決定表 / ペアワイズ / MC/DC）

### 出発点でつまずいたこと
- 前回の予告どおり`classify_triangle`に3手法を当てようとしたところ、**そもそも
  当てる先が無い**ことに気づいた。あの関数の判定点は`len({a2,b2,c2})`の3分岐と
  `c2 == a2+b2`の3分岐で、どちらも単一条件。AND/ORで束ねた複合条件が一つも無い。
  MC/DCは「1つの判定に複数の条件が含まれる」ときに意味を持つ技法なので、
  単一条件の判定では分岐網羅(C1)に退化して3手法の違いが現れない
- これ自体が9/06に整理した「3手法の違いが本当に現れるのは複合条件の部分だけ」
  の裏返しの確認になった。題材を選ぶ前に、判定の構造を先に見る必要があった

### やったこと: 同じ仕様を2通りに書き分ける
- 「二等辺三角形か否か」を、3つの真偽条件のORとして明示的に書き下した
  `isosceles_models.py`を新規作成。「どの2辺を比べるか」の決め方で2モデル用意:
  - **モデルA (sorted版)**: 3辺の2乗を昇順ソートしてから
    `eq_ab(最小==中間) or eq_bc(中間==最大) or eq_ca(最大==最小)`。
    `classify_triangle`が実際に採っている形
  - **モデルB (raw版)**: ソートせず、辺(1,2)(2,3)(3,1)をそのまま総当たり比較
- 2モデルは**同じ仕様**（どんな入力にも同じ真偽を返す）。総当たりでassertして固定した。
  違うのは書き方だけ

### 9/06の寡婦控除との構造的な違い
- 寡婦控除では条件（性別・婚姻状況…）が**そのまま関数の引数**だったので、
  決定表の1行＝テストデータそのものだった
- 今回の条件は3点の座標から**導出**される。「(T,F,F)の行をテストする」と決めても、
  それを実現する具体的な3点を別途探さないとテストが書けない。そして探して初めて
  「その行は実現不可能だった」と分かる場合がある
- この「条件ベクトルを決める」と「テストデータを作る」の分離が、今回の発見の起点

### 実測: 8通り中いくつが実現可能か（-5〜5の格子点、退化三角形を除く全組み合わせ）

```
モデルA (sorted)  eq_ab, eq_bc, eq_ca      モデルB (raw)  eq_12, eq_23, eq_31
  (T,F,F)   10,140  実現可能                 (T,F,F)    7,286  実現可能
  (F,T,F)    7,256  実現可能                 (F,T,F)    5,055  実現可能
  (F,F,F)  263,592  実現可能                 (F,F,T)    5,055  実現可能
  他5通り        0  実現不可能                (F,F,F)  263,592  実現可能
                                             他4通り        0  実現不可能
  → 3/8 通り                                 → 4/8 通り
```

- 実現不可能の理由が**2種類ある**ことが整理できた:
  - 「ちょうど2つがTrue」の3通り → 等しさの**推移律**による論理的な不可能。
    座標の範囲に依らない
  - 「3つともTrue」の1通り → 正三角形。**整数座標という入力ドメイン由来**の不可能
    （9/06に証明した性質）
  - モデルAはさらに、ソートで`a2<=b2<=c2`を保証したせいで`eq_ca`がTrueになるには
    3辺すべてが等しいしかなく、**eq_caが恒久的にFalse**になる

### 3手法を当てた結果

| | モデルA (sorted) | モデルB (raw) |
|---|---|---|
| 決定表 | 8行中3行が実現可能、5行は理由つきで不可能と明記 | 8行中4行が実現可能 |
| ペアワイズ | 3行（12ペア中7ペアしか被覆できない） | 3行（12ペア中9ペア） |
| MC/DC | **達成不能** | 4行（理論下限=条件数+1 ちょうど） |

- **決定表**: 「網羅できなかった」のではなく「網羅する対象が存在しない」ことを
  表の上に理由つきで書き残せるのが強み。実現不可能な行は、不可能であること自体を
  assertする形でテストに落とした（将来コードが変わって実現するようになったら気づける）
- **ペアワイズ**: `pairwise_gen_isosceles.py`を新規作成（9/06の`pairwise_gen.py`と
  同じ貪欲法）。制約なしで生成すると理論下限の4行が出るが、うち2行（モデルA）は
  実現する3点が存在せずテストデータを作れない。制約ありで生成し直すと、そもそも
  被覆**不可能**なペアが12ペア中5ペア（モデルA）／3ペア（モデルB）残る
- **MC/DC**: モデルBは3条件すべてで単独反転ペアが作れ、4行で達成。
  モデルAは`eq_ca`を反転させたテストデータが1つも存在せず、100%達成が原理的に不可能

### 気づき1（最大の収穫）: MC/DC未達成が「冗長なコード」の指摘だった
- モデルAで`eq_ca`のMC/DCが達成できない正体は、テスト不足ではなく**`eq_ca`が
  そもそも書く必要のない条件**だったこと。ソートで`a2<=b2<=c2`が保証されている
  以上、`eq_ca`がTrueなら`eq_ab`も`eq_bc`も必ずTrueなので、ORの結果に一切影響しない
- `(eq_ab or eq_bc) == (eq_ab or eq_bc or eq_ca)`が全入力で成り立つことを総当たりで
  assertして確認した
- DO-178Bが「到達不能コードは正当化するか除去せよ」と要求する理由が、自分の
  書いたコードで腑に落ちた。カバレッジ基準は「テストを増やせ」という指示だけでなく、
  「コードを減らせ」という診断としても機能する

### 気づき2: ペアワイズが自分の基準を満たしたまま出口の片方を通らない
- モデルBのペアワイズ3行(T,F,F)(F,T,F)(F,F,T)は、**すべて判定結果がTrue**。
  不等辺(F,F,F)＝Falseになるケースを一度も通っていない
- 貪欲法は「未被覆のペアが増えるか」だけを見るので、(F,F,F)は「新しいペアを
  増やさない」という理由で落ちる。ペアワイズは自分の網羅基準を100%満たしているのに、
  判定の出口の片方をまったく試していない
- さらに逆転が起きた: 実現可能な行が**多い**モデルB(4通り)の方が悪いテストになり、
  少ないモデルA(3通り)は偶然(F,F,F)が残って両方の出口を通った。手法の良し悪しでは
  なく偶然の産物であることが、並べて初めて見えた

### 気づき3（マネージャ視点、note記事の核候補）
- 3セッション連続で同じ主題の別ルートを踏んだ:
  - 9/06: 列挙値(3値)を複数の真偽条件に分解する**モデル化**が、MC/DCの
    「条件は独立」という前提を壊す
  - 9/06続き2: 座標を整数に限るという**入力の設計判断**が、後工程の検証項目
    (三角不等式違反・正三角形)を構造的に消す
  - 今回: `sorted()`という**実装上の一手間**が、同じ仕様のままMC/DCの達成可否を
    変える
- 共通するのは「カバレッジ率という数字は、コードの品質ではなく**コードの書き方と
  入力ドメインの組み合わせ**を映している」ということ。90%と100%を比べて騒ぐ前に、
  残り10%が『書き忘れ』なのか『原理的に到達しない』のかを見分ける必要がある。
  部下のレビューで「カバレッジが足りない」と言う前に問うべきはここだと、
  具体例を3つ持てたのが今回の一番の収穫

### テスト結果
- 新規36件（`test_isosceles_models.py`）。リポジトリ全体で155件、全てパス

### 次回予定
- モデルAの冗長な`eq_ca`を`classify_triangle`本体に照らして整理するか検討
  （現行コードは`len({a2,b2,c2})`で書いていて`eq_ca`は明示的に登場しないため、
  同じ冗長性が別の形で潜んでいないかを見る）
- （継続、積み残し）寡婦控除・ひとり親控除の`calculate_resident_tax`統合、
  TheSportsDBの件数制限の他リーグ確認、画像編のSobelフィルタ、
  テニス集計の各種テスト拡充
