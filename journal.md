# 学習ジャーナル

Python / AI駆動開発の学習記録。note記事の素材として、うまくいったことだけでなく
詰まった点・気づきもそのまま残す。

---

## 2026-07-12

### やったこと
- リポジトリ `learning-python` を作成（`git init`のみ、コミットはまだ）
- `lesson01_basics/practice.py`: 町丁目人口データの集計サンプル（変数・型・for文・if文）
  COBOL/FORTRANとの対比で解説
- `tennis_stats/tennis_stats.py`: テニス試合結果集計プログラムを作成
  - データ構造: `{"opponent": 名前, "sets": [(自分, 相手), ...]}`
  - 関数: `did_win`, `win_rate`, `win_rate_by_opponent`, `game_win_rate`
  - 当初はハードコードのリストだったが、`input()`による対話入力版に変更

### 詰まった点・気づき
- Windows環境でGit Bash経由でPythonを実行すると、日本語出力が文字化けした
  → `setx PYTHONUTF8 1` で恒久設定（次回起動セッションから有効）

### 次回予定
- テニス集計プログラムの続き（自分でキーボード入力して動作確認 / CSV等の外部ファイル読み込み版への拡張）

---

## 2026-07-15

### やったこと
- （セッション開始時に前回の記憶が引き継がれずコケた件を扱った回）

### 詰まった点・気づき
- 起動フォルダを間違え、`C:\Users\matsu`から起動した前回セッションの記憶が、
  別セッション（`AIPractice`フォルダから起動）からは見えなかった
  → 起動フォルダとメモリの紐付きを意識する必要がある、という気づき
- エディタの見方がよくわからず戸惑った（詳細未記録）
- 学習の本当の目的が「Pythonスキル習得」だけでなく、
  「還暦手前の開発マネージャがAI駆動開発を実際に手を動かして学んだときの躓き・気づきをnote記事にする」
  ことにあると明言。以降、詰まった点も含めて記録する方針に

---

## 2026-07-25

### やったこと
- 間が空いたのでセッション冒頭でこれまでの進捗をおさらい
- 詰まった点のログが記憶（メモリ）の中にしか残っていなかったため、
  リポジトリ内に振り返り用の `journal.md` を新設

### 詰まった点・気づき
- 「躓いた点を記録しておいてほしい」という依頼は覚えていたが、
  それが自分（Claude）の内部メモリにしか保存されておらず、
  本人が直接読める形になっていなかった
  → 本人が見返せる場所（リポジトリ内のファイル）に残すことの重要性に気づく

- テニス集計プログラムをコマンドプロンプトで実際に動作確認
  - 対戦相手「シナー」に対し `6-1,6-4,5-7` と入力（1行）→ 勝率が100%と表示され、
    「67%になるはず」という感覚とズレて「バグでは」と感じた
  - 原因を確認したところ、このプログラムは「1行＝1試合」という仕様で、
    カンマ区切りは同じ試合内のセット（例: 3セットマッチなら3組）を表す設計だった
  - 入力した`6-1,6-4,5-7`は「3セットマッチを1試合分入力した」ことになり、
    3セット中2セット取って試合に勝った→1試合中1勝＝100%という、仕様通り正しい結果だった

### 詰まった点・気づき
- アマチュアのテニスは1セットマッチがほとんどのため、「1行に複数セットを書く」という
  発想がそもそもなかった。仕様（1行＝1試合、カンマ区切り＝セット）を知らずに入力すると、
  「セット単位の勝率のつもり」で数字を見てしまい、結果とズレて「バグかも」と感じてしまう
  → 自分が入力しているデータの単位（試合なのかセットなのか）を意識しないまま数値を見ると、
    正しく動いているプログラムでも「間違っている」と誤解しうる、という気づき
  → 5セットマッチなら `6-1,6-3,3-6,4-6,7-6` のように5組を1行に書けばよい、という理解に到達

- 現実にはあり得ないスコアを入力して、プログラムがどこまで許容するかを試した
  - 例1: `6-3,6-3,2-6`（3セットマッチで最初の2セットを先取した時点で試合は終了するはずなのに、
    3セット目まで入力できてしまう＝矛盾したデータ）
  - 例2: `6-3,4-2`のように、片方が6ゲームに到達しないままセットが終わっている
    （通常はあり得ず、負傷等による途中棄権(リタイア)か、降雨等によるサスペンデッドのはず）
  - 現状のプログラムはスコアのバリデーションを一切行っておらず、どちらのケースもそのまま
    集計されてしまうことが分かった

### 詰まった点・気づき
- 「動くかどうか」だけでなく「あり得ないデータをどう扱うか」を試す視点は、
  開発マネージャとして要件定義・テスト設計をしてきた経験がそのまま活きる場面だった
- 実運用を意識すると、リタイア・サスペンデッドのような「スコアでは表現できない試合結果」を
  どう入力させるか（例: `DEF`という特別な表記を用意する等）という設計判断が必要になる、
  という気づきに繋がった。単純な数値集計だけでは現実の運用に耐えないケースがある

- バリデーション追加は今回は見送り、既知の制約事項として記録するに留める判断をした
  （優先度としてはCSV読み込み版などの拡張を先に進める）

- CSV読み込み版への拡張を実施
  - 設計確認: CSV列構成は「対戦相手,セットまとめ1列」（例: `シナー,"6-1,6-4,5-7"`）を採用、
    対話入力とCSV読み込みは両方残し起動時に選択する方式に決定
  - `input_matches_from_csv(filepath)`関数を追加。既存の`parse_sets`をそのまま再利用でき、
    集計ロジック側は無変更で済んだ（設計当初の狙い通り、入力部分の差し替えだけで対応できた）
  - 動作確認用に`tennis_stats/sample_matches.csv`を作成し、実行確認済み
  - matsuさん自身でもCSVファイルを作成して試す予定（次回に持ち越し）

### 次回予定
- 自分で作ったCSVファイルでの動作確認
- （時間があれば）既知の制約事項（バリデーション未対応）への対応を検討

### 既知の制約事項（未対応）
- スコアのバリデーションを行っていないため、以下のような現実にはあり得ない入力もそのまま集計されてしまう
  - 矛盾したセット数（例: 3セットマッチで2セット先取後も3セット目が続く `6-3,6-3,2-6`）
  - 6ゲーム未満でセットが終了している（リタイア・サスペンデッド等、通常のスコアでは表現できないケース）
  - 対応案: リタイア等を表す `DEF` のような特別な表記を用意する 等

---

## 2026-08-02

### やったこと
- データ標準化・データマネジメントへの興味をきっかけに、バリデーション未対応の制約事項に着手
- 設計方針を確認してから実装（AskUserQuestionで2回確認）
  1. 妥当性チェックに引っかかったデータの扱い方: 「隔離してレポート」を採用
     （ETLでいう「リジェクト/隔離」パターン。正常データだけ集計を継続し、
     問題のあった行は理由付きで別途レポート）
  2. 検証範囲: 今回は「セット単位のスコアのみ」（試合単位の整合性チェックは見送り、次回以降の課題）
- 隔離したデータの扱いについて、「異常値として除外」と「表記ゆれとして正規化し集計対象に戻す」の
  2種類があるのでは、という指摘があり、3段階の分類モデルで実装することにした
  - `valid`（正常）: そのまま集計
  - `corrected`（補正）: 全角数字（`６-３`等）を半角に正規化して採用。
    何をどう補正したかは必ずログに残す（黙って書き換えない）
  - `rejected`（却下）: 正規化しても妥当なスコアにならないもの（`6-6`など存在しないスコア、
    `7-x`など数値化できないもの）。行ごと集計から除外し、元データ（生の値）と理由をレポートに残す
- `tennis_stats.py`に`normalize_score_text`（全角→半角変換）、`is_valid_set`（セットの妥当性判定）、
  `parse_set_token`（1セット単位の分類）、`classify_line`（1試合＝1行単位の分類）を追加
- `input_matches_from_csv`を改修し、`matches`に加えて`corrected_log`・`rejected_log`を返すように変更
  （CSV読み込み経路のみ対応。対話入力は対象外のまま）
- わざと表記ゆれ・異常値を混ぜた`sample_matches_dirty.csv`を作成して動作確認
  - 全角スコア → 補正ログに記録された上で集計に反映
  - `6-6`・`7-x` → 却下ログに記録され、集計から除外
  - 既存の`sample_matches.csv`（正常データのみ）でも結果が変わらないことを確認（リグレッションなし）

### 詰まった点・気づき
- 「バリデーション」を一枚岩で考えず、「除外すべき異常値」と「正規化すれば救えるデータ」を
  区別するという発想は、データマネジメント業務そのものだと感じた
  （マネージャとしての実務感覚が、Pythonの実装設計にそのまま活きた場面）
- 生データを残さず黙って補正してしまうと、あとから「なぜその数字になったか」を追跡できなくなる、
  という点は開発マネージャとしての経験（監査証跡の重要性）とも一致していた

### 次回予定
- 対話入力（キーボード）側にも同様の検証を入れるかどうかの判断
- 見送っていた「試合単位の整合性チェック」（矛盾したセット数など）への対応検討
- リタイア・サスペンデッド用の`DEF`表記対応の検討
- ダッシュ文字違い・余分な空白など、他の表記ゆれへの補正拡張を検討するかどうか

### 詰まった点・気づき（コミット時）
- リポジトリ作成（2026-07-12のgit init）以来、一度もコミットしていなかったことに気づき、
  ここで初コミットを実施しようとしたところ「Author identity unknown」エラーで失敗
  → `git config --global user.name` / `user.email` が未設定だったことが原因
- この設定はプロジェクト単位ではなくPC（またはアカウント）につき1回でよい設定で、
  本来はGitインストール直後・最初のコミット前にやっておくべきものだった
  - コマンドライン版Gitにはこの設定を促す専用画面が存在しない
    （GitHub Desktop等のGUIツールなら初回セットアップ時に自動で聞かれる）
  - そのため「最初にコミットしようとした瞬間に初めてエラーで気づく」という経験をした
  - `user.name`を`matsu64000`、`user.email`を`matsu64000@gmail.com`に設定して解決
- 気づき: 公開リポジトリにする場合、commitに記録されるemailは誰でも閲覧可能になる点も
  合わせて説明を受けた（GitHubの`users.noreply.github.com`形式で回避可能）。
  今回は学習用ローカルリポジトリのため本メールアドレスのまま進行、公開時に再検討する方針
- 初コミット `d0884c5`（6ファイル、412行）完了

### 番外: Claude Codeの権限確認プロンプトについての気づき
- コミット前に出てくる「Do you want to proceed?」の確認プロンプトが、
  「コードレビューの依頼」なのか疑問に思い質問した
- 実際は「実行許可（メカニズム）」であり、「コードレビュー（中身の品質確認）」とは別物、
  という説明を受けた。AI駆動開発には
  1. 実行許可（設定ファイルによる許可リスト。ツール単位・コマンドパターン単位で設定可能）
  2. 判断基準（破壊的操作やpublicな領域への影響がある場合は、許可設定があっても都度確認する
     という行動規範）
  の2層構造があるとのこと
- 自治体システムの「アクセス権限テーブル」と「本番リリースのCAB承認」の対比で腹落ちした
  （権限があること＝実行してよいこと、ではないという整理）

---

## 2026-08-09

### やったこと
- 前回積み残していた「対話入力（キーボード）側にもバリデーションを入れるか」に着手する前に、
  バリデーションの考え方そのものを整理した
  - 疑問: 本来「エンティティの制約（属性・コード値範囲等）」を定義すべき一方で、
    「入力段階（キーボード／外部ファイル／その他）でのエラー検知と対策
    （オミット／アラートして修正機会を与える／一定ルールで自動修正）」も必要になる。
    Python的セオリーはどうなるか、Claudeを使うときもこの考え方をインプットすべきか、を質問
  - 整理した内容:
    1. 「エンティティの制約（何が妥当なデータか）」と「境界での対応（検知後どう反応するか）」は
       本来別物。COBOLのPICTURE句・コピー本による項目定義と、READ文後に自分で書くIF文による
       妥当性チェックが別物だったのと同じ構造
    2. 「信頼境界（trust boundary）」の外から来るデータは疑い、内側では信頼するという考え方が定石。
       境界（キーボード・CSV・将来のAPI等）は複数あっても、「何が妥当か」の判定ロジックは
       1箇所にまとめ、各境界は判定結果への「対応」だけを担当する、という分離をする
    3. 実は`tennis_stats.py`は既にこの形になっていた
       （`is_valid_set`/`parse_set_token`/`classify_line`が入力元非依存の判定ロジック、
       `input_matches_from_csv`が境界側の対応）。気づかないうちにセオリー通りの設計になっていた
    4. 判定ロジックは共通化する一方、検知後の「対応」はチャネルの特性で変えるのが自然
       （バッチ処理＝CSVは隔離してレポート、対話的＝キーボードはその場でエラーを伝えて
       再入力を促す、が定石）
    5. Claudeへのインプットについて: 積極的に伝えるべき。上記1〜4は実質的に要件定義そのもので、
       先に言葉にして渡すことで実装が「判定ロジックの一元化＋チャネル別の対応」という構造に
       なりやすい。言わずに「バリデーションして」とだけ頼むと、各所に場当たり的なチェックが
       分散しがちになる
  - この整理を踏まえ、対話入力での不正スコアへの対応方針を確認
    （即座にエラーを伝えて再入力させる／一通り入力後にまとめてエラー一覧を表示、の2択）
    → 対話的なチャネルの特性に合わせて「即座にエラーを伝えて再入力させる」を採用

### やったこと（実装）
- `input_matches`（キーボード対話入力）を改修し、CSV側と同じ`classify_line`を呼ぶように変更
  - `rejected`: その場でエラー内容を表示し、同じ行を再入力させる
  - `corrected`: 補正内容を表示した上で採用する
  - 判定ロジックそのものは新規に書かず、CSV側と完全に共有（この共通化自体が今回整理した
    セオリーの実践）
- 上記により、対話入力専用だった`parse_sets`関数が不要になったため削除
- 動作確認
  - CSV経路（`sample_matches.csv`）: 結果が変わらないことを確認（リグレッションなし）
  - 対話入力: `6-6`（存在しないスコア）→ エラー・再入力、`7-x`（数値化不可）→ エラー・再入力、
    `６-３,4-6,6-2`（全角混じり）→ 補正して採用、という一連の流れを確認

### 詰まった点・気づき
- 「バリデーションをどう書くか」より先に「バリデーションの構造をどう考えるか」を言語化したことで、
  実装そのものは非常に小さな変更（既存の判定関数を呼ぶだけ）で済んだ。設計判断に時間をかけると
  実装が軽くなる、という開発マネージャーとしての感覚がここでも裏付けられた
- 「要件定義を先に渡すとAIの実装がぶれない」という点は、部下に仕様を依頼するときの感覚と
  ほぼ同じだと感じた

### 次回予定
- 見送っていた「試合単位の整合性チェック」（矛盾したセット数など）への対応検討
- リタイア・サスペンデッド用の`DEF`表記対応の検討
- ダッシュ文字違い・余分な空白など、他の表記ゆれへの補正拡張を検討するかどうか

---

## 2026-08-09（続き）

### やったこと
- Python初心者としてのコードレビューの限界について質問
  - 整理: レビューには「要求適合性／動作結果／構造・設計／言語仕様の細部」の4層があり、
    経験がものを言う1〜3層は今の実力でも十分レビューできる。言語仕様の細部（4層目）は
    今は無理に見ようとせず、気になった書き方をその都度質問することで少しずつ広げればよい
- テストケースの妥当性・カバレッジについて質問
  - 整理: カバレッジ（行の実行割合）は正しさの保証ではない。妥当性の判断軸は実務でも使う
    「同値分割・境界値分析」がそのまま使える。カバレッジツールは「洗い出し漏れの発見」に
    使う補助であり、目標値にはしない
  - テストコード自体は「入力→期待する出力」という形で明示されるため、Python文法に詳しくなくても
    「テストが仕様を正しく表現しているか」はレビューできる、という実践的な戦略に至った
- カバレッジ基準（C0/C1/C2/MC-DC）について、COBOL/FORTRANのような手続き型言語とPython
  （特に今回のような分離を前提とした設計）とで考え方が変わるのではという質問
  - 整理: これらは全て「制御フローグラフ上のどこを通ったか」を測る構造カバレッジ。COBOLの巨大な
    EVALUATE/IFツリーは業務ロジックの複雑さが分岐数にそのまま表れるため、C1相当が実質的な
    網羅の指標になりやすかった。一方、`is_valid_set`のように小さく分解された純粋関数では、
    複雑さは分岐の数ではなく入力値の組み合わせ（値域）に移動する。分岐は2〜3個しかないので
    C0/C1は数回の呼び出しでほぼタダで100%になるが、それは境界値（`6-5`等）を試したことには
    ならない。したがって分解された設計では、構造カバレッジは「安全網（実行されていない分岐の
    発見）」に格下げし、同値分割・境界値分析を主軸に据えるべき、という結論に至った
  - `classify_line`のように複数セットの集約結果に依存する部分は、依然として分岐の組み合わせ
    網羅が意味を持つ、という使い分けも整理した

### やったこと（実装）
- `pytest`・`pytest-cov`を導入（`pip install pytest pytest-cov`）
- `tennis_stats/test_tennis_stats.py`を新規作成。上記の整理を踏まえた設計:
  - `is_valid_set`: 妥当な境界のすぐ内側／すぐ外側をペアで確認する境界値分析（`6-4`と`6-5`等）
  - `parse_set_token`: valid/corrected/rejected-format/rejected-valueの同値クラスを網羅
  - `classify_line`: 複数セットにまたがる集約の優先順位（rejectedがcorrectedより優先される
    ことを、順序を入れ替えた2パターンで確認）
  - `input_matches_from_csv`: 既存の`sample_matches.csv`/`sample_matches_dirty.csv`を使った
    回帰確認
- 24件のテストが全てパス
- `pytest --cov`でカバレッジを取得すると47%。未カバー部分は集計関数群（`did_win`等、
  境界値分析が効きにくい単純なロジック）と`input_matches`/`main`（`input()`に依存し
  単純な単体テストでは検証しづらい部分）。「リスクが高く境界値分析が効く部分に絞って
  テストを設計した結果」であり、意図的に低い数字であることを確認した

### 詰まった点・気づき
- カバレッジの数値そのものより、「どこにテストの労力を割くべきかを自分で判断できること」の
  方が重要だと実感した。47%という数字だけ見ると手抜きに見えるが、根拠を説明できれば問題ない、
  という感覚は品質保証の考え方そのもの
- C0/C1/C2/MC-DCという伝統的な基準を「古い」と切り捨てるのではなく、「どんなコードの書き方に
  対して有効な指標か」という前提条件込みで理解し直せたのが収穫だった

### 次回予定
- 集計関数群（`did_win`/`win_rate`等）への具体例ベースのテスト追加を検討
- `input_matches`（対話入力）のテストを`monkeypatch`で`input()`を差し替えて追加するか検討
- 見送っていた「試合単位の整合性チェック」（矛盾したセット数など）への対応検討
- リタイア・サスペンデッド用の`DEF`表記対応の検討
- ダッシュ文字違い・余分な空白など、他の表記ゆれへの補正拡張を検討するかどうか

---

## 2026-08-09（マネジメント視点の振り返り）

### やったこと
- 今日の一連のテスト設計の議論を踏まえ、従来の品質管理（工程ごとのテスト密度・バグ密度が
  水準値範囲か否かの定量評価＋いろんな観点での定性評価、という2段構え）が、AI駆動開発では
  どう変わるべきかを質問
  - 整理: 「定量評価が不要になる」のではなく、「定量評価の対象・分母・粒度が変わる」
    というのが正確な捉え方
  - 従来のKLOC当たりバグ密度・テスト密度が機能していた前提:
    1. コードは人間が書く → コード量が投入労力・複雑さにほぼ比例する
    2. 工程（単体/結合/総合）が明確に区切られ、フェーズごとにレビューゲートがあり、
       過去データから管理図的な水準値を蓄積できる
    3. 長期間・大人数開発で、母数となる過去実績のサンプルが十分にある
  - AI駆動開発ではこの前提が崩れる:
    - コード量と労力の比例関係が崩れる（AIは正しい定型コードを大量に一瞬で書ける。
      今日の`is_valid_set`3行 vs `test_tennis_stats.py`100行超の比較で、リスクが高いのは
      圧倒的に前者、という例で説明）
    - 工程の境目が溶ける（今日のセッション自体が「設計確認→実装→テスト→コミット」を
      数十分のループで回しており、フェーズ単位の水準値蓄積という発想が成立しにくい）
    - 結論: KLOC分母のバグ密度・テスト密度はAI駆動開発ではそのままでは意味を失いやすい
  - 一方、水準値という「マネジメントの発想の型」自体は、分母を変えれば生き残る。候補:
    - 識別した同値クラス・境界値のうち実際にテストされている割合（今日の47%カバレッジの
      逆側の実践がまさにこれ。コード行数ではなく仕様として洗い出したケースを分母にする）
    - 変更（diff）に対するテストの追随率（新しい分岐・ルールを入れたコミットに対応する
      テストが含まれているか）
    - AIが生成したコードのうち、レビューで修正が入った割合（AIの提案をそのまま採用した
      比率＝どこに人間のチェックが効いているかを可視化する新しい密度指標）
  - 定性評価の重みはむしろ増す、という整理も合わせて確認
    - 前回話した「Python初心者としてのレビューの限界」（言語仕様の細部は今の実力では
      見きれない）を踏まえると、人間が引き続き高精度で判断できるのは定性評価の領域
      （境界値の選び方が業務的に妥当か、rejected/correctedの優先順位が仕様として正しいか等）
    - 定量評価がコード量ベースから外れて頼りにくくなる分、相対的に定性評価への依存度は
      上がる、という向きで結論

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 「品質管理の定量評価という発想自体を捨てるべきか」という問いは、実は「何を分母に
  置くかを変える必要がある」という問いに言い換えられる、という気づきが今日一番の収穫
- KLOC・行数という、長年当たり前に使ってきた分母がAI駆動開発では意味を失う一方、
  「仕様として識別したリスク項目（同値クラス・境界値）」を分母にすれば、水準値という
  マネジメントの型自体はそのまま延命できる、という具体的な置き換え案が見えたのは大きい
- 定量評価が後退する分、定性評価（人間にしかできない判断）の重要性が相対的に増す、
  という逆説的な結論は、「AIに任せられる部分が増えるほど、人間の役割は軽くなる」という
  素朴なイメージとは逆で、意外性がありnote記事の題材としても強い

### 次回予定
（上記「2026-08-09（続き）」の次回予定を継続）

---

## 2026-08-11

### やったこと
- 前回終了時点で`pytest`が2件失敗していた原因を調査。`test_tennis_stats.py`が
  サンプルCSVのファイル名を`"sample_matches.csv"`のようにベタ書きの相対パスで指定しており、
  `pytest`を実行するカレントディレクトリ（`learning-python`直下か`tennis_stats`直下か）に
  よって見つかったり見つからなかったりしていた。`pathlib.Path(__file__).parent`で
  テストファイル自身の場所を基準にした絶対パスに直して解消
- 表記ゆれ補正の対象を拡張（次回予定に挙げていた「ダッシュ文字違い・余分な空白」に対応）:
  - ダッシュのバリエーション（全角ハイフン、ENダッシュ、EMダッシュ、マイナス記号、
    日本語入力で紛れやすい長音記号「ー」など）を半角ハイフンに正規化
  - セットスコア内の余分な空白（`"6 - 3"`のようにハイフンの前後に空白が入るケース）を除去
  - どちらも既存の`corrected`扱いの枠組みにそのまま乗る形で実装（新しい判定ロジックの追加は
    不要で、`normalize_score_text`の正規化テーブル・処理を拡張するだけで済んだ）
- `test_tennis_stats.py`にダッシュ表記ゆれ・内部空白のテストを追加（パラメータ化、8件）。
  `sample_matches_dirty.csv`にも該当ケース2行を追加し、動作確認用サンプルにも反映
- 全32件のテストがパス

### 詰まった点・気づき
- 「動かない」の原因が実装ロジックではなく、テストコード内の相対パス（実行時のカレント
  ディレクトリに依存）だった点。COBOLのJCLでのデータセット名解決（`ASSIGN TO`）と対比すると、
  Pythonの相対パスが「どこから実行したか」に左右されるのは感覚が違う、という気づきがあった
- 表記ゆれ補正の拡張が、既存の「正規化して比較し、差分があればcorrected」という設計のおかげで
  新しい分岐を増やさずに済んだ。最初の設計（エンティティの制約と境界対応を分離する発想、
  2026-08-09参照）がここでも効いた形

---

## 2026-08-11（続き: Webスクレイピングと非構造化データ）

### やったこと
- テニス集計プログラムの入力方法（対話入力／CSV）に加え、Webサイト（flashscore.co.jp）から
  スコアを直接取得できないか質問
  - `robots.txt`を実際に取得して確認したところ、多数のクローラー・AIボットを名指しで
    全面ブロックしており、スクレイピング禁止の意思表示が強いサイトだと判明
  - ライブスコアのような「データそのものが収益源」のサイトはスクレイピング不可なことが多い、
    という一般的な勘どころに加え、実際にrobots.txtを確認する、という2段構えのリスク検知の
    仕方を説明。「robots.txt（技術的な意思表示）」と「利用規約(ToS、法的な契約文書)」は
    別物で、robots.txtの厳しさからToSの厳しさを推測しているに過ぎない、という限界も明示
  - 「気づき方は頼もしいが、それでも人間側の確認は必要」という理解に至った
- 練習用に使えるスクレイピング用サンドボックスサイトを探索（同じくrobots.txt確認込み）
  - `scrapethissite.com`: 「The internet's best resource for learning web scraping」と明記、
    `robots.txt`も`/lessons/`と`/faq/`以外は制限なし
  - `books.toscrape.com`: 「We love being scraped!」と明記、robots.txt自体が存在しない
  - `quotes.toscrape.com`: Scrapy公式チュートリアル用に作られたサイト（Zyte社製）
  - いずれも安全に練習できると判断し、`scrapethissite.com`のホッケーチーム成績表
    （`/pages/forms/`）を今回の題材に選定（テニス集計と同じ「表形式のスポーツ統計」で
    構造が近いため）
- `requests`・`beautifulsoup4`を導入し、`scraping_practice/hockey_stats.py`を新規作成
  - `requests.get()`でHTML取得 → `BeautifulSoup`で`tr.team`ごとに`.name`/`.year`/`.wins`/
    `.losses`を抽出、という最小構成
  - 実行前に実際のHTMLをcurlで取得し、class名（`name`/`year`/`wins`/`losses`等）を
    確認してからコードを書いた（推測でセレクタを書かない）
  - 1ページ目（25チーム）の取得に成功。全24ページへの対応は次回持ち越し
  - **未コミット**（`scraping_practice/`はgit未追跡のまま。次回コミットするか要判断）
- 「音声・画像のような非構造化データはどう扱うか、データ分析のインプットになり得るか」を質問
  - 整理: 構造化データ（CSV/DB）はCOBOLのPIC句・コピー本のように事前にスキーマが
    決まっているが、画像・音声は「決まった列」を持たない生の数値の塊（画像はピクセル値の
    行列、音声は波形のサンプル値）
  - 分析に使うには「生データ→特徴抽出→固定長の数値表現（特徴量ベクトル/embedding）→
    分析」という変換ステップを挟むのが基本形。深層学習はこの変換自体もモデルに学習させて
    しまうことが多い、という現代的な補足も追加
  - 非構造化データ自体は扱いにくくても、付随するメタデータ（画像の解像度、音声の長さ等）は
    最初から構造化データであり、そこだけでも分析の足がかりになる、という実務的な視点も共有
  - 「データレイク（生データの保管）」と「データウェアハウス（構造化後の分析用データ）」の
    2層構造という整理が、マネージャ視点で一番腹落ちしたポイント

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 「Webから直接データを取ってきたい」という自然な発想に対して、技術的な可否だけでなく
  「相手の利用規約・robots.txtを先に確認する」というリスク検知プロセスそのものが
  新鮮だった様子。「気づき方の特徴」を尋ねられた際、`robots.txt`（技術的意思表示）と
  利用規約（法的契約）が別物であり、前者から後者を推測しているに過ぎないという限界を
  正直に説明したところ、「頼もしい反面、人間の確認も必要」という結論に自ら到達した
  → AI駆動開発における「AIに任せられる部分」と「人間が最終判断すべき部分」の切り分けが、
    今回はリスク検知という文脈で具体的に立ち現れた回。8/2の権限確認プロンプトの話
    （実行許可と判断基準は別、という整理）と同じ構造がここにも表れている
- 非構造化データの話は、直接のコーディング作業ではなく概念整理の回だったが、
  「データレイク／データウェアハウス」というアーキテクチャの型に落とし込んだことで、
  自治体システムでのデータ管理経験と接続できた様子

### 次回予定
- `scraping_practice/hockey_stats.py`の複数ページ対応（`?page_num=X`）
- `scraping_practice/`をコミットするかどうかの判断
- 公式APIの調査（利用規約を含めて確認する方向で）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-13（複数ページ対応とコミット整理）

### やったこと
- 実装前に`curl`で`?page_num=2`のHTMLを実際に取得し、ページネーションの仕様を確認
  （`page_num=1`〜`24`、1ページ25チーム、24ページ目のみ端数7チーム）。推測でコードを
  書かず、まず実物を見るという前回までの進め方を踏襲
- 終了判定の設計を「固定回数（`range(1, 25)`、COBOLのPERFORM VARYING相当）」か
  「動的検知（空ページで停止、PERFORM UNTIL相当）」かの2択で確認 → 動的検知を選択。
  理由: サイト側のページ数が将来変わっても壊れない設計を優先する判断
- `hockey_stats.py`に`fetch_all_teams()`を追加。`page_num`を1から増やしながら取得し、
  空ページ（`tr.team`が0件）に行き着いたら停止。あわせて`time.sleep(0.5)`で
  リクエスト間隔を空ける処理も追加（練習用サイトでも連続アクセスは避ける、という
  8/11のリスク検知の話を実装に反映した形）
- 実行して24ページ・合計582チーム分の取得を確認（24ページ目は端数7チームで正しく停止）

### 詰まった点・気づき
- Edit操作で`(1ページ目)`のような全角括弧が半角だと思い込んでツール呼び出しが3回
  失敗した。日本語コメント・メッセージの全角/半角混在は、人間が読むときは気にならないが
  機械的な文字列一致（この場合はコード編集ツール）には効いてくる、という地味な落とし穴

### 次回予定
- `scraping_practice/`と本エントリをコミット
- 公式APIの調査（利用規約を含めて確認する方向で）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-15（公式APIの調査と実践）

### やったこと
- 「公式APIの探し方・制約の確認方法・具体的な利用イメージが湧かない」という質問を受け、
  実装前にまず一般論を整理:
  - 探し方: サイトの"API"/"Developers"リンク、なければ「サイト名 API docs」で検索
  - 制約確認: APIドキュメント（技術仕様）と利用規約（法的契約）は別物として両方読む
    → COBOL対比で「他システムとのIF定義書（項目定義＋運用ルール）」と説明
  - 典型的な制約: 認証（APIキー）、レート制限、利用目的の制限、再配布可否、帰属表示
  - JSON形式のレスポンスは「項目名がデータに埋め込まれた自己記述的レコード」で、
    HTMLパース（UNSTRING的な力技）に比べて扱いやすいことも対比で説明
- 具体例として、スポーツ統計系の無料公開API「TheSportsDB」を選定し、実際にドキュメント→
  利用規約の順で読んで制約を確認（`WebFetch`で該当ページを取得）:
  - 無料テスト用キー`123`が公開されており登録不要で試せる
  - レート制限: 無料枠30リクエスト/分、超過時は`429`
  - 無料枠はアプリストア公開不可（有料サブスクリプション必須）、無許可の再配布禁止、
    スクレイピング自体も規約で禁止（＝APIを使わせる代わりに直接スクレイピングは不可）
- `curl`で`search_all_teams.php?l=NHL`・`searchteams.php?t=...`のレスポンス形式を
  先に確認してから、`api_practice/sportsdb_teams.py`を新規作成
  - `hockey_stats.py`と対になる構成（`fetch_teams_by_league()` → `main()`）
  - `requests.get()` → `response.json()`で辞書化するだけで、`BeautifulSoup`によるHTML
    解析が不要になる点を実際のコード量の差で体感
- 実行してNHLチーム一覧を取得、動作確認済み

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 実行してみたところ、NHL（本来32チーム）のはずが**10チームしか返ってこなかった**。
  生のAPIレスポンス自体が10件だったため、コード側のバグではなく無料テスト用キー`123`
  固有の件数制限と判明。この制限はドキュメント・利用規約のどちらにも明記されておらず、
  **実際に動かして初めて発覚する制約**だった
  → 8/11（robots.txtと利用規約の限界）・8/13（設計判断の文脈情報はコードから読めない）
    と同型で、「ドキュメントを読む」だけでは制約の全体像は分からず、動かして初めて
    見える部分が必ず残る、という気づきが4度目に立ち現れた回。COBOL時代の「テスト環境と
    本番環境でマスタデータの件数が違い、テストでは気づけない制約があった」経験と
    重なる、とのコメントあり

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-15 続き（有名APIの比較、GitHubへの初push）

### やったこと
- TheSportsDB以外の有名API（GitHub API、OpenWeatherMap、Google Maps Platform）を
  `WebFetch`でドキュメントを読み比べ、認証方式・レート制限・課金モデルの違いを整理:
  - GitHub API: 未認証60回/時間、認証済み5,000回/時間（登録の重さが軽い）
  - OpenWeatherMap: APIキー登録制、60回/分・100万回/月の無料プラン
  - Google Maps Platform: APIキー取得に**Google Cloudの課金設定（クレジットカード登録）が
    必須**。無料プラン（超過しても止まるだけ）と、無料枠＋従量課金（超過すると自動課金）の
    2種類があるという課金モデルの違いが今回の一番の発見
- GitHub APIの認証なしアクセスを自分のリポジトリで試したいという流れになったが、
  GitHubアカウントを持っているか本人も記憶が曖昧だった
  - ローカルのgit設定・SSH鍵・Windows資格情報マネージャーを確認したが手がかりなし
  - 「認証なしでユーザー存在を確認できる」というGitHub APIの性質をそのまま使い、
    `GET /users/matsu64000`を叩いたところ200 OKでアカウント実在を確認（作成日
    2026-07-11、`public_repos: 0`）。ちょうど今回学んでいる内容そのもので確認できた
- `learning-python`をこのアカウントに初めてpush。`gh` CLI未導入・保存済み認証情報も
  なかったため、リポジトリ作成はブラウザ、pushはGit Credential Manager経由のブラウザ
  ログインで、ユーザー本人に`!`プレフィックスで直接実行してもらう形を取った
  - 空リポジトリのデフォルトブランチが`main`だったため、ローカルの`master`を
    `git push -u origin master:main`で対応付け
- push後、`api_practice/github_repos.py`を新規作成し、`GET /users/{username}/repos`で
  自分のリポジトリ一覧を認証なしで取得。実行結果は`learning-python (public, branch: main)`

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- push直後にAPIで確認したところ、`pushed_at`は更新されていたが`size`フィールドだけ
  しばらく`0`のままだった。contents APIで中身自体は正しく届いていることを確認できたため、
  「サイズ集計だけ非同期のバッチ処理で遅延する」というAPI側の実装都合と判断。
  これも8/15前半と同型の「ドキュメントに書かれていない、動かして初めて分かる挙動」で、
  同じ日に2回続けて同じパターンに遭遇したことになる
- 「アカウントを作った記憶があるが自信がない」という曖昧な状態から、ローカルの手がかり
  （git設定・SSH鍵・資格情報マネージャー）を先に確認し、それでも分からなければ対象の
  外部サービス自身のAPI（今回はGitHubの未認証ユーザー検索）で存在確認するという段取りは、
  自治体システムで「本人が古いアカウントの存在を忘れていた際、まず手元の台帳を確認し、
  それでも不明なら当該システムに直接照会する」という調査の順序と同じ構造。学習中のAPIを
  そのままメタ的に「調査の道具」として使えた回

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- 非構造化データ（画像等）を実際にPythonで読み込んでみる（`Pillow`/`numpy`等）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応

---

## 2026-08-15 続き2（非構造化データ: 画像をPillow/numpyで読む）

### やったこと
- `Pillow`・`numpy`を新規導入
- 進め方の確認: 「Pillowで自作する」という選択が、実際にはClaudeがコードを書いて実行する
  形になったことについて、本人から「自分は何もしていないのでは」という疑問が出た
  → 「自作」は『手元の写真ファイルを使う』との対比で、"画像データをコードで生成する"
    という意味だったと説明。あわせて[[教え方の指針]]に沿い「動くコードを先に見せて
    から解説する」進め方であることを改めて確認し、①コードを読んで理解する路線か、
    ②構造だけ示して値の穴埋めは本人が書く路線かを聞いたところ、①（読解中心）を継続する
    ことで合意
- `image_practice/pixel_basics.py`を新規作成。2行4列・8ピクセル分のRGB値を明示的に
  指定し、`numpy.array(pixels, dtype=uint8)` → `Image.fromarray()`で画像化、
  `sample_image.png`として保存。あわせて`numpy.array(image)`で読み戻し、
  `.shape`・`.dtype`・各ピクセル値を1つずつ表示
  - COBOL対比: `array.shape`の`(2, 4, 3)`は`OCCURS 2 OCCURS 4`のR/G/B(各`PIC 9(3)`)
    に相当する入れ子構造として説明
  - `image.size`が`(4, 2)`（幅×高さ、Pillowの慣習）、`array.shape`が`(2, 4, 3)`
    （高さ×幅×チャンネル、numpyの慣習）で軸の順序が異なる点を、実際に両方printして
    比較することで確認（ドキュメントだけでは体に入らない類の話）
- `image_practice/feature_extraction.py`を新規作成。8/11に整理した「生データ→特徴抽出→
  固定長の数値表現」の流れを`sample_image.png`で実践:
  - グレースケール変換（`image.convert("L")`）で3チャンネル→1チャンネルに圧縮。
    単純平均ではなくPillow標準の輝度計算式（緑を重く、青を軽く見る重み付け）で
    変換されることを、実際の出力値から確認
  - `array.mean(axis=(0,1))`でチャンネルごとの平均値（3要素の固定長ベクトル）を算出。
    COBOL対比でコントロールブレイクの合計行に相当すると説明
  - `array.flatten()`で2次元構造を1次元ベクトル（要素数24）に展開。機械学習モデルへの
    入力形式としてよくある前処理であることも補足
- 生成物`sample_image.png`は`pixel_basics.py`を実行すれば再現できるため、既存の生成物
  除外方針（`.coverage`等と同様）を踏襲し`.gitignore`に追加、コードのみコミット

### 詰まった点・気づき（マネージャ視点、note記事の核候補）
- 「Pillowで自作する」という選択肢名から、本人はコードを自分の手で書くことを想定していた
  様子。実際にはClaudeが書いて実行するいつもの進め方だったため、選択肢の言葉と実際の作業
  分担の間にズレが生じた。指摘を受けてから役割分担（読解中心か実践中心か）を明示的に
  聞き直したことで解消したが、「AIとの協働で誰が何を『やった』ことになるのか」という、
  AI駆動開発特有の役割認識のズレは、部下への作業指示でも起こりうる（指示の言葉が曖昧だと
  誰が手を動かすか誤解される）ため、記事の題材候補としてメモ

### 次回予定
- テスト用キー`123`の件数制限が他のリーグでも同様に起きるか確認（原因の切り分け）
- （継続）集計関数群のテスト追加、`input_matches`のmonkeypatchテスト、試合単位の整合性
  チェック、`DEF`表記対応
- 非構造化データの続き（音声データを扱ってみる、または画像の特徴量を使った簡単な分析）
